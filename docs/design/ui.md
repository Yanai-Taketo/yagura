# デザインシステムと画面設計

- 層: 全体設計書（常に現在形。[docs/README.md](../README.md) の 2 層構造に従う）
- 根拠 ADR: [ADR-0003](../adr/0003-ui-policy.md)（Blazor Interactive Server 単一モード・デザインシステム先行・MudBlazor・ライト基調 + ダーク切替・文言原則・利用しやすさの下限）
- 関連文書: [architecture.md](architecture.md)（§4 観測性・§6 検索防御の表示側）、[database.md](database.md)（§2.2 時刻の契約・§5.3 昇格案内・§6.1 切替予告）、[configuration.md](configuration.md)（§1 フォールバック表示・§3 反映方式表示——いずれも §5.4 で引き受ける）
- 本文書の範囲: デザイントークン・共通コンポーネント規約・画面レイアウトの骨格・状態表示の原則・用語対応表・アクセシビリティ基準・UI メトリクス・MudBlazor の統治。次の事項は他文書の管轄とする:
  - circuit 上限の値・認証失効の反映・掲示用途の失効配慮 → [security.md](security.md)
  - 個別画面の詳細設計（項目配置・遷移図） → 実装設計（本文書の骨格と規約に従う）
- 数値の扱い: architecture.md と同じ運用。未確定は [§12 確定待ち一覧](#12-確定待ち一覧)（UI-x）で追跡する。**デザイントークンの値は設計判断として本文書で確定する**（実測対象ではない）が、コントラスト基準（§8）の充足検証を通るまでは調整があり得る（UI-1）

## 1. デザインシステムの原則

- **すべての画面はデザイントークン（§2）と共通コンポーネント（§3）だけで組む**。ページ固有の CSS・JavaScript・インラインスタイル（`Style=` 属性）は原則禁止（ADR-0003 決定 2）
- **例外の手続き**: ページ固有であるべき理由を PR に明記し、[§11 ページ固有 CSS 例外一覧](#11-ページ固有-css-例外一覧) に登録した上で認める。例外は共通化の候補であり、共通コンポーネントへ吸収されたら一覧から消し込む
- **MudBlazor の低レベル API をページに散乱させない**（ADR-0003 決定 3 の緩和策）: 頻出パターンは共通コンポーネントへ集約し、ページは共通コンポーネントを組み合わせる。MudBlazor を直接使ってよいのは共通コンポーネントの実装内部と、単純な静的レイアウト部品（コンテナ・グリッド等）に限る
- **色・フォント・余白の直接指定を禁止する**: すべてトークン経由で参照し、テーマ切替（§2.4）で破綻しない構造を保つ（ADR-0003 決定 4）

## 2. デザイントークン

### 2.1 配色

方向性は「クリーンな業務系・ライト基調」（ADR-0003 決定 4）。値は §8 のコントラスト検証（UI-1。M8-2 / Issue #69 で実施済み——§8 の検証記録参照。ライトの state-warning / state-info は初期値が未達だったため調整済み）を通して確定した。**トークン値は本文書を正とし、実装のテーマ定義を変更する PR は本表の更新を同じ PR に含める**（configuration.md §8 と同型の同期規約）。

| トークン | ライト | ダーク | 用途 |
|---|---|---|---|
| primary | #1565C0 | #90CAF9 | 主要アクション・リンク・選択状態 |
| background | #F5F7FA | #121212 | 画面背景 |
| surface | #FFFFFF | #1E1E1E | カード・テーブル・ダイアログの面 |
| text-primary | #1A1A1A | #E0E0E0 | 本文 |
| text-secondary | #5F6B7A | #A0A0A0 | 補助テキスト・ラベル |
| divider | #D7DDE4 | #3A3A3A | 罫線・区切り |

**状態色**（意味を固定し、これ以外の意味で使わない）:

| トークン | ライト | ダーク | 意味 |
|---|---|---|---|
| state-ok | #2E7D32 | #81C784 | 正常・成功（受信中・保存成功） |
| state-warning | #A05F00 | #FFB74D | 警告・要注意（一時保管への退避中・上限接近・縮退運転）。ライトは UI-1 検証で #B26A00 から調整（§8） |
| state-error | #C62828 | #E57373 | 異常・失敗（受信断・書込失敗・破棄発生） |
| state-info | #0270B2 | #4FC3F7 | 情報（案内・昇格提案）。ライトは UI-1 検証で #0277BD から調整（§8） |

- **色だけで意味を伝えない**: 状態は常にアイコンまたはテキストを伴う（色覚特性への配慮。§8）
- severity（syslog の重大度）の表示色は状態色と別系統のトークンとして実装設計で定義する（Error 以上 = state-error 系、Warning = state-warning 系に揃える）
- **実装形態**: トークンは MudBlazor のテーマ機構（テーマ定義のパレット）への割当を第一経路とし、独自の CSS 変数の併設は共通コンポーネントの実装内部に限る。具体の割当は §2.1.1 のとおり確定した（UI-8。M8-1 / Issue #68）

#### 2.1.1 MudBlazor テーマへの割当（UI-8 確定。2026-07-06）

実装側の唯一のテーマ定義は `src/Yagura.Web/Theming/YaguraTheme.cs`（MudTheme）。トークンは PaletteLight / PaletteDark の両方へ同名項目で割り当てる。割当が実際に CSS 変数（`--mud-palette-*`）として描画されることは実サーバ起動 + 描画 HTML の走査で確認済み（例: `--mud-palette-primary: rgba(21,101,192,1)` = #1565C0。確認日 2026-07-06）。

| トークン | MudBlazor パレット項目 |
|---|---|
| primary | Primary |
| background | Background |
| surface | Surface |
| text-primary | TextPrimary |
| text-secondary | TextSecondary |
| divider | Divider・LinesDefault・TableLines（罫線系 3 項目へ同値割当） |
| state-ok | Success |
| state-warning | Warning |
| state-error | Error |
| state-info | Info |

派生割当（独立トークンを増やさず、既存トークンの値を流用する項目）:

- AppbarBackground / DrawerBackground ← surface、AppbarText / DrawerText ← text-primary（共通骨格の面は「カードと同格の面」として扱う）
- PrimaryContrastText と各状態色の ContrastText（塗り面上の文字色）← ライトは #FFFFFF 固定、ダークは text-primary のライト値 #1A1A1A（ダークの状態色は明色のため文字は暗色）。コントラスト充足の検証は UI-1 の対象に含める
- 上表・派生割当にない MudBlazor パレット項目（Secondary・Tertiary 等）は MudBlazor 既定値のまま残るが、**トークンに存在しない色のため画面・共通コンポーネントから使用してはならない**（§1 の直接指定禁止と同じ扱い。§3.2 受け入れ条件①の「参照はテーマ由来の値とトークンに限られる」はこの限定を含む）

### 2.2 タイポグラフィ

- フォントは OS 提供のフォントスタックとし、Web フォントを同梱・参照しない（閉域完結・ADR-0003 決定 1 の npm/CDN 非依存と同じ向き）: `"Yu Gothic UI", "Segoe UI", Meiryo, sans-serif`。等幅（ログ本文・生データ表示）: `Consolas, "Courier New", monospace`
- **非 Windows の閲覧クライアント**（閲覧リスナは LAN 公開であり、閲覧側の OS は保証されない）では総称ファミリへのフォールバックとなることを許容する
- **フォントスタックの実在検証（UI-9 確定。2026-07-06）**: Windows 11（10.0.26200）の `System.Drawing.Text.InstalledFontCollection` で "Yu Gothic UI"・"Segoe UI"・"Meiryo"・"Consolas"・"Courier New" の 5 指名フォントすべての実在を確認した。非 Windows クライアントは指名フォントが解決されず末尾の総称ファミリ（sans-serif / monospace）へフォールバックする（前項の許容のとおり。総称ファミリの表示品質は閲覧側 OS の既定に委ね、個別検証はしない）。MudBlazor 9.6.0 の同梱 CSS はフォント名を一切ハードコードせず、すべてテーマ由来の CSS 変数（`--mud-typography-*-family`）を参照するため（実ファイル走査で Roboto 等の出現 0 件。§10）、テーマ定義のフォントスタック設定がそのまま全域に効く。MudBlazor 公式ドキュメントが案内する Google Fonts（Roboto）の `<link>` は追加していない（本節の「Web フォントを参照しない」の受け入れ検証は §10 ②が兼ねる）。描画 HTML に `--mud-typography-default-family: 'Yu Gothic UI', 'Segoe UI', Meiryo, sans-serif` が出力され、Roboto への参照が 0 件であることを実サーバで確認済み
- サイズ段階（px）: 12（注釈）/ 14（本文・既定）/ 16（強調・小見出し）/ 20（画面見出し）/ 28（数値の大表示——ダッシュボードのカウンタ等）。行間は 1.5 を基準とする
- **サイズ段階の MudBlazor Typo への割当（UI-9 の一部として M8-1 で確定）**: caption・overline = 12 / Default・body1・body2・subtitle2・button = 14 / h6・subtitle1 = 16 / h5 = 20 / h4 = 28。**h1〜h3 は h5 と同値（20）に固定**し、トークン外サイズの混入経路を塞ぐ（画面見出しは h5 を標準とする）。等幅スタック（ログ本文・生データ表示）は MudBlazor Typography に対応枠がないため、ログ表示系の共通コンポーネントの実装内部で適用する（§2.1 実装形態の「独自 CSS 変数の併設は共通コンポーネントの実装内部に限る」の範囲）

### 2.3 余白・形状

- 余白は **4px 基準のスケール**（4 / 8 / 12 / 16 / 24 / 32）のみ使用する
- 角丸は 4px（入力・ボタン）と 8px（カード・ダイアログ）の 2 値。影は「なし（フラット）/ カード / ダイアログ」の 3 段のみ使う
- **影 3 段の elevation 割当（UI-9 確定。2026-07-06）**: なし = elevation 0 / カード = elevation 1（MudPaper・MudCard の既定値であることを MudBlazor 9.6.0 の API ドキュメント（同梱 XML）で確認）/ ダイアログ = MudDialog の内蔵影（同梱 CSS の `.mud-dialog` が Material elevation 24 相当の `box-shadow` を直接保持することを実ファイルで確認。テーマの Shadows 配列では変更されない）。3 段の意味論は「フラット / 面 / 浮遊」であり、**浮遊系の部品（メニュー・ポップオーバー等。MudBlazor 既定 elevation 8）は「ダイアログ」段（浮遊）に属するものとして部品既定値を許容する**。ページ・共通コンポーネントが `Elevation` を明示指定してよい値は 0 と 1 のみ。テーマの Shadows 配列自体は MudBlazor 既定（Material 26 段）のまま使い、利用面をこの規約で 3 段に限定する
- **角丸 2 値の制約と方式（UI-9 で判明・確定。2026-07-06）**: MudBlazor のテーマ機構が持つ角丸は 1 値（`LayoutProperties.DefaultBorderRadius` → `--mud-default-borderradius`）のみで、本節の 2 値をテーマだけでは表現できない。割当: テーマ既定 = **4px**（入力・ボタン。描画 HTML への出力を実サーバで確認済み）。カード・ダイアログの 8px は、カード / ダイアログの共通コンポーネントの実装内部で適用する（§2.1 実装形態の許容範囲）——**M8-3 で適用済み**: カード = `YaguraCard`（`.yagura-card` 規則）、ダイアログ = 共通コンポーネント内部スタイル（`yagura-components.css` の `.mud-dialog` 規則。ダイアログはポータル描画のため部品側のラッパクラスでは面に届かず、ダイアログ段の形状規約として同スタイルシートで適用する）

### 2.4 テーマ

- 既定はライト。**ダークは同格のテーマ**であり、全トークンが両テーマの値を持つ（ライトの機械変換で作らない。ADR-0003 決定 4——ダッシュボードの暗所掲示運用を想定ユースケースに含む）
- 切替は UI 上のトグルで行い、**選択はブラウザ側（端末ごと）に保存する。サーバへは書き込まない**——閲覧リスナは認証なしで閲覧者の概念を持たず（ADR-0004 決定 2）、書き込みエンドポイントも持たない（§4）。端末ごとの保存は「掲示用の大型モニタはダーク・手元 PC はライト」の使い分けとも整合する
- OS 設定への追従は選択できる項目とし、既定では手動選択を尊重する（勝手に切り替わらない）
- **OS テーマ追従の JS 手書きなし実現可否（UI-9 確定: 可。2026-07-06）**: MudBlazor 9.6.0 の `MudThemeProvider` が `GetSystemDarkModeAsync` / `WatchSystemDarkModeAsync` を提供し、同梱 JS（MudBlazor.min.js）内の `matchMedia("(prefers-color-scheme: dark)")` で実現されていることを実ファイルと API ドキュメント（同梱 XML）で確認した。手書き JS の追加なしで追従を実装できる（ADR-0003 決定 1 と両立。方式の再設計は不要）。`MudThemeProvider` の自動追従（`ObserveSystemDarkModeChange`。既定 true）は**無効化して使う**——既定の手動選択尊重と衝突するため、追従は利用者が「OS の設定に合わせる」を選択した場合に限り `WatchSystemDarkModeAsync` の購読で行う
- **切替 UI と保存の実装（M8-1 で確定）**: 共通骨格（MainLayout）のアプリバー右端のメニューで「ライト / ダーク / OS の設定に合わせる」の 3 択。既定はライト。選択はブラウザの localStorage（キー `yagura.theme-mode`）へ保存する——標準 API の呼び出しは `IJSRuntime` 経由で行い、手書き JS ファイルは追加しない（ADR-0003 決定 1 の範囲内。§5.2 のような例外登録は不要）。保存済み選択の復元は circuit 確立後（prerender 中は JS 不可）のため、ダーク保存端末では初回表示の一瞬だけ既定のライトが見える——既定がライトである以上、v0.1 はこれを許容する（暗所掲示運用で問題が反復するなら再評価）

## 3. 共通コンポーネント規約

### 3.1 標準形（v0.1 の初期セット）

実装参照は M8-2（Issue #69）で実装した共通コンポーネント（`src/Yagura.Web/Components/Common/`）。表示文言は同ディレクトリの `UiText`（§7 の文言カタログ）に集約する。

| コンポーネント | 規約 | 実装参照 |
|---|---|---|
| ボタン | 主ボタン（primary 塗り）は 1 ビューに 1 つまで（**ビュー = 同時に視認される単位。ページ本体・ダイアログ・ウィザードの 1 ステップはそれぞれ別のビュー**）。破壊的操作（削除・昇格確定等）は state-error 系 + 確認ダイアログ必須。**クリック処理の実行中は自動で無効化する（二重送信ガード。await 中の再クリックが古い VersionToken で保存に到達する楽観競合を構造的に防ぐ——Issue #372）。ハンドラから漏れた例外は state-error 通知に受け、circuit エラーにしない（第一の受け皿は各画面の catch）。管理系の保存ボタンは MudButton 直書きではなく本コンポーネントを使う** | `YaguraButton`（Role=Destructive は確認ダイアログ表示を内蔵し、確認内容なしでは実装エラーになる。二重送信ガード + 例外の受け皿を内蔵） |
| フォーム | ラベルは入力の上。検証エラーは項目直下に文言で表示（色だけにしない）。必須項目は「必須」表記（記号 * だけにしない） | `YaguraFormField`（共通枠）/ `YaguraTextField`（1 行テキスト入力。`Masked` でパスワード等の秘密入力——入力値を画面に表示しない。昇格ウィザードの SQL 認証で追加）/ `YaguraSelectField`（選択。ネイティブ `select` をトークンで整形——「指定なし」を常に選べる。**必須の二者択一**は `AllowEmpty=false` で「指定なし」を外せる——絞り込み条件でない設定値の選択で使う）/ `YaguraCheckboxField`（オン/オフ。ネイティブ `checkbox` をトークンで整形。昇格ウィザードのサーバ証明書の信頼で追加）/ `YaguraDateTimeField`（日時。ネイティブ `datetime-local`。M8-3 で追加） |
| テーブル（ログ一覧） | 軽量射影列のみ表示（architecture.md §6）。**仮想化またはページングを必須とし、結果上限件数の一括レンダリングを行わない**（転送量の防御 = 射影、描画量の防御 = 仮想化の 2 本立て）。行クリックで詳細へ。**打ち切り（結果上限到達）は表の末尾に件数と共に明示する** | `YaguraTable<TItem>`（ページング内蔵。行選択指定時はキーボードで到達できる詳細ボタン列を自動追加——§8） |
| 通知（トースト） | 一時通知は自動で消えるが、**state-error の通知は手動で閉じるまで残る**（見逃し防止）。「閉じる」はその閲覧者の画面からのみ消えるセッションローカル操作であり、サーバ状態を変更しない。通知履歴は状態画面から辿れる | `IYaguraNotifier` / `YaguraSnackbarNotifier`（ページは ISnackbar を直接使わない。通知履歴は状態画面 = M8-3） |
| 空状態 | 「データがない」画面は、無地ではなく**次に取るべき行動**を必ず示す。特にログ未着の空状態は、**このサーバの受信先情報（IP アドレス・ポート番号）をコピーできる形で表示し**、届かないときの切り分け文書（configuration.md CF-D5）への導線を置く（インストーラが閉じた 30 分動線の続き——機器設定 → 最初の 1 件——を空状態画面が引き受ける） | `YaguraEmptyState`（NextAction 必須——無地の空状態を構造上作れない）+ `YaguraCopyField`（コピー可能な受信先表示） |
| 確認ダイアログ | 取り消せない操作は、何が起きるかの要約 + 明示的な確認ボタンで構成する。既定フォーカスは安全側（キャンセル）に置く | `YaguraConfirmDialog`（Esc で閉じられる = キャンセル） |
| 状態帯（ヘルスバー） | ダッシュボード・全画面共通ヘッダに置く現在状態の帯（§5.1）。state-ok / warning / error の 3 状態 + 一言サマリ | `YaguraStatusBand` |
| カード（M8-3） | カード面の共通部品。角丸 8px（§2.3）と影のカード段（elevation 1）を実装内部で適用する。ページは MudPaper を直接使わない | `YaguraCard` |
| 数値の大表示（M8-3) | ダッシュボード・状態画面のカウンタ/ゲージ現在値。数値は 28px 段（§2.2）。整形（単位・桁区切り）は利用側 | `YaguraStatCard` |
| 時間軸チャート（M8-3） | 受信量の推移（棒）+ 受信断区間（帯の重ね描き。§5.3）。外部チャートライブラリなしの手書き SVG。色はトークン由来の CSS 変数のみ・受信断は色 + 凡例文言のセット（§2.1・§8）。近似断点は薄い帯で区別 | `YaguraTimelineChart`（入力型 `YaguraTimelineBucket` / `YaguraTimelineInterval`） |
| 送信元アドレス（ADR-0007） | 送信元 IP の表示 3 箇所（検索一覧・検索詳細・ダッシュボード送信元別）の共通形。IP が主（等幅・常に表示）・逆引きホスト名が補助（注釈サイズ・text-secondary・等幅にしない）。由来ツールチップ（§4 の確定文言）と aria-label を内蔵。引けない場合は IP のみ | `YaguraSourceAddress`（`IReverseDnsResolver` のキャッシュ充填 + 束ね通知で反映） |

### 3.2 部品追加の提案手順 — 外部貢献の入口

（ADR-0003 決定 2。database.md §1.3 の provider 追加と同じ考え方。手順の詳細は CONTRIBUTING）

- 共通コンポーネントの追加・変更は、**本文書 §3.1 の表の更新を同じ PR に含める**提案として行う（Issue → 設計対話 → PR）
- 受け入れの技術条件: ①トークン（§2）のみで実装されている（**判定形: hex 値・任意の色/サイズの直書きがなく、参照はテーマ由来の値とトークンに限られる**）、②ライト・ダーク両テーマで §8 の基準を満たす、③画面文言が §7 の用語対応表・文言原則に従う、④ページ固有 CSS を持ち込まない
- 同型の UI が 2 画面以上に現れた時点で共通化を検討する（3 回目の複製を作らない）

## 4. 画面レイアウトの骨格

- 全画面共通の骨格: **上部アプリバー**（製品名・状態帯・テーマ切替）+ **左ナビゲーション**（画面一覧。折りたたみ可）+ **コンテンツ領域**。この骨格自体も共通コンポーネントとする
- v0.1 の画面構成と責務:

| 画面 | 責務 | 主な設計委任元 |
|---|---|---|
| ダッシュボード | 健康状態の一目確認（§5.1）・受信量の推移・重大度分布・受信量上位の送信元（Top talkers）・流量制限の発火上位送信元（Issue #288。カード型——「取りこぼし（送信元ごとの受信量の制限）」の増加から「どの装置の暴走か」へ進む view。受信量の view（Top talkers・送信元別の受信状況）とは「受信量 vs 制限発火」の軸違いであることと、値の生存期間（ゲートの有界バケットと同寿命——起動からの累計ではない。architecture.md §3.3）をカードの説明文で明示する）・送信元別の受信状況・スプール/カウンタの現在値 | architecture.md §4.6・§3.3 |
| ログ検索 | 絞り込み強制なしの検索（期間・送信元・重大度の閾値・facility・解析状態・自由文）・軽量一覧・詳細表示・受信断区間の時間軸表示・検索条件の URL 共有（Issue #148） | architecture.md §6・§4.4 |
| システム状態 | 全カウンタ・ゲージの一覧・通知履歴・受信断履歴。**試用報告様式に対応したコピー機能**（出力には平易語と併記でカウンタ識別子——開発用語側のキー——を含め、報告と設計文書を機械的に突合できるようにする。様式の項目一覧・コピー形式は [UI-7](#12-確定待ち一覧)） | architecture.md §4.1・ADR-0006 基準 2 |
| 設定（ウィザード群） | 初期セットアップ・本番昇格・設定変更・circuit 管理（一覧・個別切断）・フォワーダ配布キットの生成（管理リスナ経由のみ）。**実装参照（M8-4 / Issue #71 の骨格 + ADR-0008 のキット生成画面。以後の管理画面追加を含む全ルート）**: `/admin`（入口）・`/admin/setup`・`/admin/promotion`・`/admin/circuits`・`/admin/reload`・`/admin/forwarder-kit`・`/admin/login`・`/admin/auth-setup`（[ADR-0010](../adr/0010-admin-ui-authentication.md)）・`/admin/remote-access`（[ADR-0012](../adr/0012-admin-https-cert-ui.md)）・`/admin/ingestion-tls`（[ADR-0019](../adr/0019-ingestion-tls-cert-ui.md)）・`/admin/email-notification`（[ADR-0017](../adr/0017-email-notification.md)）・`/admin/source-silence`（[ADR-0018](../adr/0018-source-silence-detection.md)）（`src/Yagura.Web/Administration/Screens/`。**管理画面のルート追加は本一覧の更新を同じ PR に含める**——閲覧系ページの L-5 許可リスト運用と同じ向き）。管理リスナ帰属は「エンドポイントへの Admin メタデータの機械的付与（名前空間由来）+ circuit 層の帰属検査（共通レイアウト `AdminScreenLayout`）」の二層（security.md §1 L-5 の覆域の限界への対処）。**再起動待ちキーの常設カード（Issue #286。2026-07-18 オーナー裁定）**: 設定の再読み込み（configuration.md §3）で検出された「反映にサービス再起動が要るキー」の累積集合を、入口 `/admin` に常設カード（検出時刻併記）で表示する。表示先は認証済み管理面のみ——閲覧面には出さない（管理系情報を閲覧面に露出しない情報設計判断。L-5 類似）。プロセス内状態のため再起動で自然に消える。**保存先縮退の常設バナー（[ADR-0023](../adr/0023-upgrade-availability.md) 決定 1。Issue #466）**: 保存先のスキーマ初期化に失敗して縮退運転中である間、同じ入口 `/admin` に常設バナーを出す——起動を失敗させずに縮退させる以上、縮退していることが必ず見えるのが成立条件であり、メール通知未設定の環境では「管理画面を開いた瞬間に分かる」ことが現実的な発見経路になる。**失敗の詳細（保存先名・例外文字列）はバナーに出さない**（出所は Windows イベントログ 1039 へ寄せる）。あわせてログイン画面（`/admin/login`）は、この縮退による失敗だけを資格情報の誤りとは別の文言で示す——同じ汎用文言に丸めると、利用者がパスワードを忘れたと誤解してリセットを試み、それも失敗して混乱するため。アカウントの実在有無に依存しない事実であり、[ADR-0011](../adr/0011-app-auth-failure-backoff.md) 決定 3 の列挙耐性は損なわない。**左ナビゲーションへの管理リンク表示（2026-07-06 試用フィードバック）**: 共通骨格（`MainLayout`）は circuit のリスナ帰属（`YaguraCircuitContext.IsAdminListener == true`）のときだけ設定・昇格・接続管理・キット生成へのリンクを出す——**閲覧リスナ（`false`）・判定不能（`null`＝prerender 中含む）では出さない安全側**。到達可否自体は `AdminScreenLayout` のガードが担保するが、閲覧リスナに管理導線を見せないことでも不要な露出を避ける | configuration.md §3〜§7・database.md §6.1・security.md §2.2・[ADR-0008](../adr/0008-forwarder-kit-generation.md) |

- 閲覧系（ダッシュボード・検索・状態）は閲覧リスナ、書き込み系（設定）は管理リスナに属する（configuration.md §4.2）。**閲覧リスナはいかなる書き込みエンドポイントも持たない**（テーマ保存は §2.4 のとおりブラウザ側、通知の「閉じる」は §3.1 のとおりセッションローカル）。この不変条件は [security.md](security.md) §1 L-5 の CI 検証対象として仕様化済み
- **外形監視用の liveness エンドポイント `/health`（Issue #126。2026-07-09 オーナー決定）**: 画面ではなく機械向けの固定応答（200 + 固定文字列 `OK`）。対応メソッドは GET と HEAD のみ（外形監視ツールには既定で HEAD を送るものがあるため。HEAD は本文なし・ヘッダ同一）。認証なし・DB 非依存・内部状態を一切含まない——外形監視・LB のヘルスチェックからの死活確認用途に限定する。実装参照: `src/Yagura.Web/YaguraWebViewerExtensions.cs`（`MapYaguraWebViewer`）。L-5 許可リストに登録済み
- **閲覧 3 画面の実装参照（M8-3 / Issue #70）**: ダッシュボード = `src/Yagura.Web/Components/Pages/Dashboard.razor`（ルート `/`）・ログ検索 = `LogSearch.razor`（`/search`）・システム状態 = `SystemStatus.razor`（`/status`）。共通骨格（アプリバー + 左ナビゲーション + **全画面共通の状態帯**——§5.1 の「ダッシュボード最上部（および全画面の状態帯）」は骨格 1 箇所への常設で満たし、二重表示しない）は `Components/Layout/MainLayout.razor`。閲覧系ページのルート追加は L-5 の許可リスト（`ViewerEndpointAllowlistTests`）の更新を同じ PR に含める。データ取得は読み取り専用の口のみ——`ILogStore` の読み取り操作（M8-3 で追加した詳細取得・システムイベント読み出し・送信元別集計を含む。database.md §1.2）と、ホスト管轄の観測値の読み取り契約 `IYaguraSystemStatusReader`（`Yagura.Abstractions.Observability`。書き込み系マーカー `IYaguraWriteService` を実装しない——L-5 の参照分離検査の対象）。ページが MudBlazor を直接使うのは単純な静的レイアウト部品（MudGrid・MudItem・MudText 等のコンテナ・タイポグラフィ）に限る（§1 の範囲内）
- **送信元別受信状況の表示粒度（UI-4 確定。M8-3）**: 送信元アドレス単位で「最終受信時刻・最後に受信してからの経過時間・保存件数」を**最終受信時刻の古い順（無音の疑いが強い順）**に全送信元分一覧する（集計は provider 側 `QuerySourceActivityAsync`——量の上位 N 抽出ではない）。閲覧の防御は上限 1,000 送信元 + ページング（20 件/ページ）で行い、**上限打ち切りで切り捨てられるのは「最近まで受信できている送信元」側**（昇順のため。無音化検出を上限が損なわない）。打ち切り時はその旨を注記する
- **途絶検知のウォッチリスト表示と設定画面（[ADR-0018](../adr/0018-source-silence-detection.md) 決定 4。Issue #351。2026-07-19 実装）**: ①UI-4 一覧の既存カードに、ウォッチリスト登録済み送信元の**登録済みマーク**（表示名つき chip。途絶中は色を error に切り替え）と、無音時間列の**途絶中の強調 chip** を追加した——新しい画面は作らず既存カードを拡張する（判定は周期評価——1 分間隔——の結果であり最大 1 分の遅延を持つ）。**閲覧面には登録導線を置かない**（登録は管理操作。#286 の「管理系情報を閲覧面に露出しない」裁定と同じ向き）。②ウォッチリストの管理は認証済み管理面の設定画面（`/admin/source-silence`）で行う: **登録は受信実績のある送信元からの候補選択を主経路**とし（最終受信時刻・累計件数を添える。IP の転記ミスによる「形式は正しいが違うアドレス」の黙った空振りを構造的に減らす）、手入力は先回り登録用に残す。**閾値は値 + 単位（分/時間/日）で入力し**（分の直打ちを強要しない——委任 5 の裁定）、新規登録では明示確定を必須とする（既定 24 時間の意味は閾値ヘルプに明記）。保存されているのに稼働状態に現れない（手編集の不備でエントリ単位無効化された）エントリは「無効（設定を確認）」として一覧に見せる——「監視されているつもりで監視されていない」を画面で検出できるようにする（決定 1 の識別表示）
- **重大度分布・Top talkers の追加（M8-5/Issue #159。2026-07-09 実装）**: ダッシュボードに欠けていた「平常時からの逸脱検知」（severity の内訳が見えない）と「フラッディング検知」（受信量上位の送信元を掴む view が無い）の 2 視点を追加した。①**重大度分布**（`QuerySeverityDistributionAsync`）: 観測窓（受信量推移と同じ直近 1 時間）内の重大度別件数を一覧表示する。表示は `YaguraSeverityChip`（§4「調査動線と重大度の可読化」と同一の色対応: 0〜3 = state-error / 4 = state-warning / 5〜7 = 通常表示）+ 件数で、severity 昇順・PRI 解析失敗（severity 不明）バケットは末尾に表示し解析失敗の事実を隠さない（§5.3 と同じ向き）。②**Top talkers**（`QueryTopTalkersAsync`）: 観測窓内の受信量上位 10 送信元を件数降順で別セクションに表示する。既存の「送信元別の受信状況」（UI-4。最終受信が古い順・全送信元・量の上位 N 抽出は不可）とは**別メソッド・別カード**とし、UI-4 の制約に抵触しない（無音化検出の一覧を上位 N 表示に置き換えない）。各行から `/search?source=` への 1 クリック導線を持つ点は既存の送信元別受信状況と揃える。**窓の必須化（Issue #145 への配慮）**: Severity・SourceAddress 列には索引が無く、無条件の集計は大規模時にフルスキャンへ劣化し得るため、両集計とも索引済みの ReceivedAt 範囲（観測窓）への絞り込みを必須とする設計とした（複合索引の追加自体は Issue #145 の範囲。database.md §1.2 M8-5 追加分）
- **調査動線と重大度の可読化（M8-3 追補。2026-07-06 オーナー指示の UX 改善）**: ①検索結果・詳細の重大度は数値のまま出さず、短形ラベル + 状態色で表示する（`YaguraSeverityChip`。**色の対応: severity 0〜3 = state-error / 4 = state-warning / 5〜7 = 通常表示**。ラベルは §7 の選択肢ラベルと同一の対応表 `UiText.SeverityShortLabels`——二重管理しない。範囲外の値は解釈を偽装せず生値のまま表示する）②ダッシュボードの送信元別受信状況の各行から、当該送信元を初期条件にしたログ検索へ 1 クリックで遷移できる（`/search?source=<アドレス>`——「静かになった装置に気づく → その装置のログを見る」の調査動線を途切れさせない。リンク遷移のみでサーバ状態を変更しない = §4 不変条件の範囲内）③詳細表示の到達性（2026-07-06 試用の実フィードバック——詳細を結果テーブルの下に開く方式は、行が多いと「押しても何も起きない」ように見えた）: 詳細は**オーバーレイ（モーダルダイアログ）で表示**し、結果一覧の位置に関わらず必ず前面に出す（inline `MudDialog` を可視状態にバインド。Esc・背景クリックで閉じ、フォーカス復帰は MudBlazor のダイアログ機構が担う——§8）。行末の詳細ボタンには tooltip「詳細を表示」を付ける（アイコンだけでは意味が伝わらない）④詳細オーバーレイのレイアウト（案 A = 色帯ヘッダー。2026-07-06 オーナー選択）: 重大度に応じた**色帯ヘッダー**（`YaguraSeverityBand`——重大度アイコン + 色 + 長形ラベル + ファシリティ補助チップ + 右端の閉じるボタン。色区分は ①と同一。帯の淡い背景は `--mud-palette-{error|warning}-rgb` から低透過度で作り両テーマ対応）を最上部に置き、**メッセージ本文を主役**（`YaguraCopyBlock`——等幅枠 + コピー）、時刻・送信元などの**メタ情報は 2 次情報として 2 列**（`.yagura-detail-meta`）、生データ・構造化データは**控えめ枠 + コピー**（`YaguraCopyBlock` の `Muted`）に整理する⑤ファシリティ（syslog PRI の facility）の表示（同オーナー指示）: 一覧列・詳細とも「番号: 名前」（例 `3: デーモン`。`UiText.FormatFacility`。RFC 5424 Table 1）で表示する。**16〜23（local0〜7）は運用側が機器ごとに自由に使う枠**であり標準名を持たないため、severity のような固定意味づけはせず**番号を主・名前を補助**とする（対応表外の番号は名前を付けず番号のみ＝解釈を偽装しない）
- **検索条件の拡充と URL 共有（Issue #148。syslog 実務者ペルソナの深掘りレビュー）**:
  - **重大度は完全一致ではなく閾値方式**（`SeverityAtMost`。§7.2「severity → 重大度」の用語はそのまま）: syslog は数値が小さいほど深刻なため、「N 以上の重大度」という運用語彙は「選択値と、より深刻な値をすべて含む」条件になる。以前の完全一致は「3: エラー」を選ぶと severity 0〜2（より深刻な緊急・警報・重大）が結果から消える実害があったため、閾値方式へ置き換えた（完全一致・閾値の切替 UI は設けない——完全一致の需要は DB-6 確定後の自由文検索拡張で再評価する）。選択欄には補足文言（`UiText.SearchFieldSeverityHelp`）で挙動を明示する
  - **facility 絞り込み**（`UiText.SearchFieldFacility`）: 0〜23（RFC 5424 Table 1）の完全一致選択。表示は §5.1 決定の「番号: 名前」対応表（`UiText.FormatFacility`）を再利用し、二重管理しない
  - **解析状態（ParseStatus）絞り込み**（`UiText.SearchFieldParseStatus`）: 解析済み / 解析失敗 / 不完全の完全一致選択。重大度条件は severity が未設定（PRI 未解析）の行を常に除外するため、「解析失敗だけを見る」はこの条件が唯一の手段になる
  - **自由文検索の対象列拡張は本 PR のスコープ外**: [database.md](database.md) §1.2 DB-6 が確定した対象は Message のみ——Hostname/AppName/StructuredData 等への拡張は別途の提案を要する
  - **検索条件の URL クエリへの双方向反映**: 期間・送信元・重大度・facility・解析状態・自由文のすべてが URL クエリに載り、URL の共有・ブックマーク・ブラウザの戻る/進むで検索状態を再現できる。実装は Blazor の `[SupplyParameterFromQuery]` + `OnParametersSetAsync`（検索ボタンは現在の条件を URL へ `NavigateTo` するだけで、実際のクエリ実行は `OnParametersSetAsync` に一本化する——二重実行を避ける単一経路。`LogSearch.razor` 参照）。改ざん・手編集された URL の不正な値は例外を出さず「条件なし」に安全側で丸める
- **保存検索（利用者ごとの検索条件のサーバ側保存）は設けない（2026-07-16 オーナー裁定。Issue #274）**: 検索条件の保存・再利用・共有は、上記の URL クエリ双方向反映（Issue #148）によるブラウザのブックマーク・URL 共有で代替する——この代替関係を本項で明示し、空白のまま放置しない。閲覧 UI 認証（ADR-0010 Phase 4）の導入で「利用者ごと」のサーバ側保存は技術的に成立するようになったが、保存先スキーマ・共有可否・認証なし構成での挙動という設計コストに対し、URL 方式が検索の再現という利用目的を実用上満たしているため、作らないことを決定として記録する。**再評価トリガ**: 利用者から「URL ブックマークでは足りない」具体的要望が挙がったとき（例: 保存したい条件の組が多数でブラウザ側の管理が破綻する・検索画面内からの条件切替の需要・チーム間でサーバ側に共有したい需要）
- **ログ検索結果の CSV エクスポート（Issue #157）**: 検索結果一覧の下に「CSV エクスポート」ボタン（`YaguraButtonRole.Normal`——主ボタンは検索ボタン 1 つのみに保つ。§3.1）を置き、**現在の検索条件を引き継いだクエリ付きで** `/search/export.csv` へ全ページ遷移する（`NavigationManager.NavigateTo(url, forceLoad: true)`。フォワーダ配布キット生成ボタンと同じ方式）。仕様:
  - **RFC 4180 準拠**（引用符・カンマ・改行を含むフィールドの引用）+ **UTF-8 BOM 付与**（Excel の日本語文字化け耐性）+ **CSV インジェクション対策**（セル先頭が `=`・`+`・`-`・`@` の場合に `'` を前置し、表計算ソフトによる数式解釈を防ぐ。実装: `Yagura.Web.Export.CsvField` / `LogRecordCsvWriter`）
  - **件数上限は画面の一覧表示と同じ 10,000 件**（architecture.md §6 M-10 の仮値をそのまま踏襲——CSV だから画面より多く出す、という非対称を作らない）。上限到達時は応答ヘッダー `X-Yagura-Csv-Truncated: true` を付与する（CSV 本文は監査提出用途のためデータ行のみに保ち、非データ行を混入させない）
  - **全文出力**: 一覧射影の 200 文字切り詰め（database.md §2.1）は回避し、メッセージは全文を出力する（`LogQuery.MessageProjectionLength` の呼び出し側オプションを使用——database.md §1.2 の射影契約自体は変更しない）
  - **メモリ節約**: CSV 全体を文字列へ組み立てず、レコード 1 件ごとに応答ストリームへ直接書き出す
  - **対象は閲覧リスナの GET のみ**（`/search/export.csv`。L-5 許可リストに登録済み——security.md §1）
  - **検索条件の対応範囲**: 検索画面の全条件（期間・送信元・重大度閾値・facility・解析状態・自由文——Issue #148 の拡充分を含む）を、検索画面の URL クエリと同じキー（`from`・`to`・`source`・`severity`・`facility`・`parseStatus`・`q`）で引き継ぐ。不正な値は検索画面と同じく例外を出さず「条件なし」に安全側で丸める
  - **CSV の列構成は検索条件セットから独立**: 出力列（受信時刻・送信元・プロトコル・重大度・ファシリティ・ホスト名・アプリ名・ProcId・MsgId・構造化データ・メッセージ全文・解析状態・送信元時刻・レコード ID）は `LogRecordSummary` の全項目の固定射影であり、重大度・ファシリティ・解析状態は当初から列に含まれている——絞り込み条件の追加（Issue #148）で列を増減しない
- **検索結果の「さらに読み込む」（database.md DB-11。カーソルページング。Issue #144 残課題。2026-07-10 実装）**: 結果上限（10,000 件。M-10 仮値）に達した検索は、従来は期間や条件を絞る以外に続きを見る手段がなかった——障害調査の基本動作「該当時間帯を過去へ遡って読む」ができなかった（Issue #144 の根拠）。これを解消するため、結果一覧の直下に**追記型**の「さらに読み込む」ボタン（`YaguraButtonRole.Normal`。§3.1「主ボタンは 1 ビューに 1 つ」を守り検索ボタンのみを Primary のまま維持する）を追加した。設計判断:
  - **追記型（ページ送りではなく末尾への追加）を採用**: `YaguraTable` は既に読み込み済みの結果（最大 10,000 件）をブラウザ内で 50 件/ページに仮想ページングする仕組みを内蔵済み（§3.1）——これは「読み込み済みデータの中の移動」であり、DB-11 が解く問題（**10,000 件の壁を越えて過去へ読み進める**こと）とは別の層にある。ページ送り（既存ページを置き換える方式）ではなく追記型（末尾に連結して同じ一覧内で見える化する）を選んだ理由は、調査中に「さっき見ていた行」を画面上に残したまま連続的に過去へ辿れることを優先したため——障害調査は一つの検索結果内を行き来しながら読むことが多く、ページ境界で一覧がリセットされると調査の連続性が切れる
  - **カーソルは URL に載せない**: 検索条件（期間・送信元・重大度・facility・解析状態・自由文）は Issue #148 のとおり URL クエリの正であり続けるが、**追記の読み込み位置（カーソル）は画面のローカル状態に留め、URL には反映しない**。理由は 2 つ——①カーソルは「たまたまその時点で表示していた最後の行」というセッション依存の一時的な位置であり、検索条件のような「再現可能な入力」とは性質が異なる（新着ログが継続的に増える対話的検索で、過去に記録したカーソル位置を後日 URL から再現しても、その間に挿入された新しい行との整合性は保証できない）。②URL 共有・ブックマークの利用目的は「この条件で検索を再現する」ことであり、「特定のスクロール位置を再現する」ことではない——後者まで URL に含めると共有 URL が「今の検索条件」なのか「特定時点の読み込み位置」なのか利用者に紛らわしくなる。新しい検索（検索ボタン押下・ブラウザの戻る/進む・共有 URL を開く）は常に先頭ページから開始する
  - **状態管理**: 「まだ続きがあるか」（`_hasMore`）は直近に取得したバッチの件数が上限（10,000 件）ちょうどだったかで判定する——累積表示件数（`_results.Count`）で判定すると、追記を繰り返すうちに「まだ続きがあるか」を表せなくなるため（4 回読み込んで 40,000 件表示中でも、直近バッチが 10,000 件未満なら打ち切りは尽きている）。打ち切り明示の文言（`UiText.MissingDataTruncatedFormat`）も「1 回のクエリの上限」ではなく「現在までの累計表示件数」を案内する表現に改めた（誤読防止）。**既知の境界ケース（許容。PR #221 レビュー指摘の記録）**: 該当件数がちょうど上限の倍数で尽きる場合、最終バッチが上限件数ちょうどのため `_hasMore` が真のまま残り、次の「さらに読み込む」が空バッチ（0 件）を返して初めてボタンが消える——**1 回だけ結果の増えない空の読み込みが発生する**。事前に判定するには「上限 + 1 件を取得して 1 件捨てる」等のクエリ・判定の複雑化を要する一方、発生条件は稀（件数が上限の正確な倍数）で実害は 1 クリックの空振りに留まるため、単純さを優先して許容する
  - **時間軸チャート・受信断区間の再計算**: 「さらに読み込む」後は表示範囲が過去へ広がるため、時間軸チャート（§3.1 `YaguraTimelineChart`）・受信断区間の重ね描きも拡張後の全件を基準に再計算する（期間指定（`from`）がある検索では、その下限を優先するため範囲は広がらない——通常どおりの挙動）
  - **CSV エクスポート（Issue #157）は変更しない**: エクスポートは「現在の検索条件全体を対象にした一括出力」であり、画面の対話的な追記読み込みとは目的が異なる（調査中に少しずつ遡るのではなく、確定した条件で全量を一度に取り出す）。カーソルページングを導入しても、エクスポートは従来どおり画面と同じ上限（10,000 件）までを一括出力する現行方式を維持する——カーソルを使った分割エクスポートは本 PR のスコープ外（必要になれば別途提案する）
- **送信元の逆引き（PTR）ホスト名の併記（[ADR-0007](../adr/0007-reverse-dns-display.md)。2026-07-06 設計確定）**: 送信元 IP アドレスを表示する 3 箇所——検索結果一覧の送信元列・検索詳細の送信元メタ・ダッシュボードの送信元別受信状況の送信元列——で、逆引きホスト名を**補助表示として併記**する。規約:
  - **IP が主・名前が補助**: IP アドレスは従来どおり等幅で常に表示し、逆引き名は補助テキスト（text-secondary・注釈サイズ）として添える。名前は等幅にしない（IP との視覚区別）。引けない場合（PTR 未登録・打ち切り・対象帯域外・機能オフ）は従来どおり IP のみ＝表示の欠落を異常に見せない
  - **由来の明示**: 逆引き名は syslog HOSTNAME（送信者の自己申告——詳細表示の「ホスト名」）とは**別由来の名前**であり、食い違い・片方欠落があり得る。逆引き名の表示にはツールチップで由来を添える——**ツールチップは 3 箇所すべてに共通で付与し、確定文言は本節を正として `UiText` に収載する**（「送信元 IP アドレスから DNS の逆引きで取得した名前です。機器自身が名乗るホスト名（ログに記載）とは別で、食い違うことがあります」）。詳細表示の既存ラベル「ホスト名 / アプリ名」は「**ホスト名（ログに記載）/ アプリ名**」へ改め、両者を画面上で区別できるようにする（§7.2 用語対応表に両行を追加済み——同表の逆引き行の説明は本節の確定文言を指す）
  - **解決対象**: プライベート/サイトローカル系の帯域の IP のみ（ADR-0007 決定 2——グローバル IP はクエリを発しない）。かつ**解決要求の発行は画面に現に描画されている行（ページング後の 1 ページ分・仮想化ウィンドウ内）の IP に限り**、結果上限件数（検索 10,000 件・送信元一覧 1,000 件）への一括解決要求は行わない
  - **描画は解決を待たない**: 表示はキャッシュ命中分のみ即時、未解決分は非同期解決の完了時に反映する（IP の表示・画面操作を DNS がブロックしない）。**反映は解決完了 1 件ごとではなく束ねて行う**（circuit 越しの再描画を連発させない。束ねの周期・キャッシュ・資源保護の仮値は [UI-10](#12-確定待ち一覧)）
  - **無害化**: 逆引き名は送信元が自分の PTR で操作できる untrusted 値（経路上の DNS 応答改ざんもあり得る）。プレーンテキストとして描画し、ホスト名として妥当な文字集合（RFC 1123 の LDH——英数字・ハイフン・ドット）以外を含む名前・253 文字超の名前は**表示しない**（切り詰め・整形をして見せない）。**`xn--` で始まるラベル（Punycode）は Unicode へ復号せず原文のまま表示する**（ホモグラフ偽装を復号表示で助けない）。逆引き名を検索条件・リンク URL・認可判定に自動使用せず、**監査記録・診断ログ・Windows イベントログのいずれにも出力しない**（ADR-0007 決定 5）
  - **検索条件には使えない**（v0.1）: 検索の送信元欄は従来どおり IP アドレスのみ。名前が見えるのに名前で検索できない誤解への手当てとして、送信元欄のラベル・補足で IP 指定であることを明示する
  - **機能オフ時**（`Viewer:ReverseDns:Enabled` = 無効）: DNS クエリを発しないことに加え、**キャッシュ済みの名前の表示も行わない**（「オフ = 逆引き名は出ない」を単純に保つ。ADR-0007 決定 4）
  - **共通コンポーネント化**: 表示は共通コンポーネント `YaguraSourceAddress`（§3.1）に集約する（§1 の低レベル API 散乱禁止・同型 UI 3 箇所の共通化。ADR-0007 のペルソナレビュー（PR #98）が §3.2 の「設計対話」を兼ねた）
  - **実装参照（ADR-0007 実装 PR）**: 解決・キャッシュ = `Yagura.Web.ReverseDns.ReverseDnsResolver`（契約 `IReverseDnsResolver` は `Yagura.Web` 内に配置——Web の外から参照する利用者が存在しないため `Yagura.Abstractions` へは置かない。M6-4 の判断枠組み。キャッシュは `ConcurrentDictionary` + 固定 TTL——BCL は DNS レコードの TTL を取得できない）。下位解決 API の集約点 = `IReverseDnsLookup` / `SystemDnsReverseLookup`（**オフ時・対象帯域外で下位 API へ到達しないことを `ReverseDnsResolverTests` が固定**——security.md §1.1 の受け入れ条件）。カウンタ = `Yagura.Web.Diagnostics.ReverseDnsMetrics`（状態画面が 4 種を平易語と併記して表示する。表示補助の観測でありメタデータ領域への永続化はしない）。数値の仮値は `ReverseDnsResolverLimits.Default`（[UI-10](#12-確定待ち一覧) に記録）
- **構造化データ（RFC 5424 STRUCTURED-DATA）の一覧接頭表示（2026-07-07 オーナー承認済みデザイン）**: ログ検索一覧のメッセージ列で、構造化データを本文の**直前に原文のまま**（例 `[winevt Channel="System" EventID="7036"]`）淡色・等幅で表示する。**保存・解析は変更しない**（database.md §2.1 の保存契約はそのまま——一覧射影（`LogRecordSummary`）に含めて返す表示層の変更のみ）。**表示上の省略のみ**（単一行・最大 32 文字相当・はみ出しは省略記号——切り詰め・整形をしてデータを変えることはしない。全文は詳細表示の構造化データ欄（`YaguraCopyBlock`）で見られる）。構造化データを持たないレコード（RFC 3164 送信元等）では接頭表示自体を出さない
- **フォワーダ配布キット生成画面（[ADR-0008](../adr/0008-forwarder-kit-generation.md)。実装参照 `src/Yagura.Web/Administration/Screens/ForwarderKitScreen.razor`、ルート `/admin/forwarder-kit`）**: 宛先は NIC 候補（`INicCandidateSource`。ループバック・リンクローカル/APIPA・無効化 NIC を除外済み）のラジオリスト + 手入力で選択する。**既定選択は置かない**（設計条件 1——到達可能性の判断責任は管理者に残ることを画面上の注記でも示す）。収集チャネルは System・Application が初期オン、Security は初期オフのオプトインで、有効化時は機微情報・量の注意を表示する（設計条件 2）。生成される ZIP の内容一覧・検証済み Fluent Bit 版（`Yagura.Web.ForwarderKit.ForwarderKitConstraints.VerifiedFluentBitVersion`）・MSI 非同梱時の注記を画面に静的表示する。生成ボタンはクエリ付き GET（`/admin/forwarder-kit/download`）へ `NavigationManager.NavigateTo(url, forceLoad: true)` で遷移する——最終検証は必ずサーバ側（`ForwarderKitRequest.TryCreate`）で行い、画面側の検証は早期フィードバックに過ぎない
  - **MSI オプトイン同梱（設計条件 9。2026-07-07 amendment）**: 配置フォルダ（`IForwarderMsiSource.FolderPath`。データルート配下 `forwarder`）の**フルパスを常時表示**する。検出状態で画面を出し分ける——未検出: 配置案内（期待ファイル名パターン）+ パス表示のみ、チェックボックスは出さない。複数検出: エラー表示 + 検出ファイル名一覧、同梱不可。単一検出: 「MSI を同梱する」チェックボックス（**初期オフ = オプトイン**）を表示し、オンにするとファイル名・版（ProductVersion 優先）・SHA256・公式ハッシュ照合結果・ZIP サイズ予告（20 MB 超）を表示する。**検証済み版と異なる場合は警告表示 → 明示確認チェックボックス（二段階）**を要し、未確認のまま生成を試みるとサーバ側（`ForwarderKitRequest.TryCreate`）が 400 で最終防御する。生成 URL には `includeMsi` を付与する

## 5. 状態表示の原則

### 5.1 健康状態の一目確認（状態帯）

- ダッシュボード最上部（および全画面の状態帯）に「**稼働中 / 警告あり / 異常あり**」の 3 状態を常時表示する
- **判定は累積カウンタの現在値ではなく、直近の観測窓内の増分・現在のゲージ状態で行う**（過去の一過性破棄が永久に警告を灯し続けないため。窓幅・復帰条件・確認済み操作の要否は [UI-6](#12-確定待ち一覧)）。事象と状態の対応:

| 状態 | 判定入力（例） |
|---|---|
| 稼働中（state-ok） | 受信リスナが開いており、観測窓内に破棄・障害事象がなく、未消化のスプールデータも残っていない |
| 警告あり（state-warning） | **現在、未消化のスプールデータが残っている**（一時保管への退避が進行中） / 上限・容量の接近 / 縮退運転中 / 証明書期限接近 |
| 異常あり（state-error） | 観測窓内の破棄（いずれかの破棄カウンタの増分）/ 保存先障害の継続 / 受信リスナが開けていない |

- 一過性事象で状態帯が復帰しても、**発生の事実は通知履歴・受信断履歴（状態画面）に残る**——状態帯は「今」を、履歴は「起きたこと」を担う
- **一時保管への退避の復帰は「消化完了」で判定する（観測窓の時間経過ではない。M8-3 追補。Issue #132）**: 退避データの DB 格納が確定してからスプールのセグメントを削除する仕組み（architecture.md §3.2.1・§3.2.4 の at-least-once）を踏まえ、スプールの現在使用量が 0 に戻った時点を「消化完了」の正シグナルとして扱う。これにより「退避 → すぐ drain → DB 格納完了」のケースでも、観測窓（仮値 5 分）の経過を待たずに直ちに警告が解除される。**取りこぼし（異常あり）は対象外**——破棄は取り消せない事実であり「消化」に相当する正シグナルが存在しないため、引き続き観測窓内の増分（時間経過による自然な鎮静化）で判定する。累計カウンタ自体はリセットしない（監査上の価値を残す）——「今」の状態表示と「過去の累計」の分離は §5.4・ダッシュボードの現在値カード（`YaguraStatCard` の補足文言）が担う
- 正常時は肯定形で表示するが、**観測できる範囲に限って言い切る**——対象と時間の両方を限定する: 「稼働中——現在、サーバに届いたログの取りこぼしは発生していません」（①ネットワーク上のロスはサーバから観測できない——architecture.md §4.5 / D-2 と矛盾する断言をしない、②判定は観測窓内であり累積の主張をしない——窓外に出た過去の破棄は履歴側で確認する。「状態帯は『今』、履歴は『起きたこと』」の分担のとおり）。「稼働中」はポートの待ち受けを意味し、ログが現に届いていることは意味しない——**全送信元合算の最終受信時刻を併記**し、送信元別の受信状況（無音化の検出は §12 UI-4）への導線を置く
- 実装参照（M8-2）: `YaguraStatusBand`（3 状態 + サマリ + 最終受信時刻の併記 + 送信元別への導線。判定自体はサーバ側観測性の管轄で、部品は表示のみを担う）
- 実装参照（M8-3）: 状態帯は共通骨格（`MainLayout`）が全画面へ常設し、更新間隔 10 秒（UI-2 の仮値）で自律更新する。判定の暫定実装は `Yagura.Host.Observability.SystemStatusReader`——観測窓は仮値 5 分（UI-6。読み出し履歴との差分で「窓内の増分」を判定。異常 = 破棄系カウンタの増分・警告 = スプール使用量の上限接近（仮値 80%）・スプールなし縮退）。「受信リスナが開けていない」の判定入力は観測点が未整備のため未対応（リスナを開けない失敗は現状ホスト起動失敗として現れる——UI-6 の確定と合わせて観測点を追加する）。最終受信時刻は保存済みの最新 1 件の受信時刻（スプール滞留分は反映が遅れ得る——観測できる範囲の値）
- 実装参照（M8-3 追補。Issue #132）: 一時保管への退避の警告（`YaguraHealthReason.SpoolEvacuationObserved`）のみ、観測窓を使わず**スプールの現在ゲージ（`DiskSpool.CurrentUsageBytes > 0`）**で判定するよう変更した（上記「消化完了」）。副次効果として、観測窓の基準（baseline）が無い最初の読み出し（プロセス起動直後）でも判定できる——前回セッションからの未消化セグメントの持ち越しを見逃さない。ダッシュボードの「一時保管への退避（累計）」カードは、累計は保持したまま、現在も進行中か（`現在も一時保管への退避が進行中です`）・消化完了済みか（`退避分は格納済み（現在は正常です）`）を補足文言で出し分ける（`UiText.StatSpoolEvacuatedOngoingSupplement` / `StatSpoolEvacuatedResolvedSupplement`。取りこぼしカードの補足 `LossSupplement` と同じ「進行中か過去かを見分けられるようにする」設計方針）。**復帰そのものはイベントログへ通知しない**（判断: architecture.md §4.6 の能動通知は「対応が必要な事象」を無人時間帯にも気づけるようにする仕組みであり、自己回復（消化完了）は対応不要であるうえ、退避 → drain の反復が多い局面では通知が急増し抑制窓の設計意図（ノイズ削減）に反するため。1004 = スプール退避の継続の警告は既存どおり残り、状態帯・ダッシュボードのリアルタイム表示が日中の確認手段を担う）

### 5.2 最終更新時刻とステール警告

- **ダッシュボードは最終更新時刻を常時表示する**（ADR-0003 帰結）。表示が古くなった場合は、**画面全体にステール警告を重ねる**（時刻表示の色変化だけにしない——暗所の大型モニタで遠目に分かること）。閾値は仮値: 更新間隔の 3 倍（UI-2）。**起点はクライアントが最後にデータを受信した時刻**とする
- **ステール検知と警告の描画はクライアント側で自律動作させる**: サーバ停止・circuit 回復不能の局面でこそ警告が要るため、サーバ側レンダリングに依存できない。これは ADR-0003 決定 1 の「分離 JS モジュール」例外として**本文書で設計上確定した例外**であり、§11 の台帳（ページ固有の例外）とは区別する
- **ステール警告の文言は受信への影響を必ず含める**——ただし観測できる事実だけを言う: サーバの状態が確認できない場合は「表示が古くなっています（最終更新: ◯◯）。サーバの状態を確認できないため、ログの受信状況も不明です」。接続は維持されているのに表示の更新が止まっている場合も、受信の継続は保証できない（更新経路が異常な局面である）ため「画面とサーバの接続は維持されていますが、表示の更新が止まっています。ログの受信状況はこの画面から確認できません」とする。「受信は継続しています」と言い切ってよいのは §7.3 の再接続中（ブラウザとサーバ間のリンク断であり、サーバ健在を前提にできる）に限る
- 役割分担: **画面のステール警告は在室者の誤読防止**を担い、**無人時間帯の実効通知はイベントログ経由の既存監視**（architecture.md §4.6）が担う
- 実装参照（M8-2）: `YaguraStaleGuard`（最終更新時刻の常時表示 + 全画面オーバーレイ）+ 分離 JS モジュール `src/Yagura.Web/wwwroot/js/stale-guard.js`（本節が設計確定した例外の実装位置。監視タイマーと警告表示はブラウザ内で完結し、サーバと通信しない）。閾値の既定は更新間隔の 3 倍（UI-2 の仮値のまま。パラメータで上書き可）。circuit 生存中に更新だけが止まった局面の文言出し分けは更新経路を持つ画面側（M8-3 ダッシュボード）が引き受ける——**M8-3 で実装済み**: ダッシュボードの定期更新（10 秒。UI-2 仮値）が失敗した周期で「画面とサーバの接続は維持されていますが、表示の更新が止まっています。…」（本節の確定文言。`UiText.StaleWhileConnectedNotice`）を警告表示し、次の周期で自動再試行する

### 5.3 欠けているデータの明示

「無い」の理由を画面で見分けられるようにする:

- **受信断区間**（architecture.md §4.4）: 検索結果・ダッシュボードの時間軸上に区間として表示し、クラッシュ由来の近似断点はその旨を印す
- **保持地平**（database.md §2.3）: ログの保持期間より古い時間範囲を検索した場合、「この範囲は保持期間外（削除済み）」を結果に明示する。保持地平より古い受信断区間の表示は誤読（「区間以外はログがあったはず」）を招くため、保持地平の明示とセットでのみ見せる
- **検索の打ち切り**（architecture.md §6）: 結果上限・タイムアウトによる打ち切りは、件数・条件を絞る案内と共に表示する
- **保持期間の常時明示**（database.md §3）: ダッシュボードに「◯日より古いログは自動的に削除されます」の現在値を常時表示する。ファーストラン完了画面でも案内する
- 実装参照（M8-2）: `YaguraMissingDataNotice`（受信断区間・クラッシュ由来の近似注記・保持地平・検索の打ち切りの文言明示）/ `YaguraRetentionNotice`（保持期間の常時明示）
- 実装参照（M8-3）: 時間軸グラフ上の区間描画 = `YaguraTimelineChart`（§3.1。ダッシュボードの直近 1 時間・検索結果の時間軸の両方で受信断区間を帯として重ね描き。近似断点は薄い帯 + 一覧側の注記文言で印す）。保持地平の明示は検索画面が「検索範囲の下限なし・または下限が保持地平より古い」場合に必ず表示する（受信断区間の通知より上に置き、セット表示の規約を満たす）。保持期間が未適用（不正値フォールバック = 削除しない。database.md §3）の場合も「自動削除は行われない」ことをダッシュボードで常時明示する（`UiText.RetentionDisabledNotice`——削除の有無を画面から判別できない状態を作らない）

### 5.4 設定・システム由来の警告と案内

- configuration.md §1 のフォールバック（キー名・不正値・適用値の 3 点）と未知キー一覧は、設定画面と状態画面の両方に表示する（イベントログを見ない利用者にも届かせる）
- **反映方式の表示**（configuration.md §3 の引き受け）: 設定変更時に「即時反映 / 接続の瞬断あり / サービス再起動が必要」を操作前に示し、再読み込みの結果で**未反映のまま残った項目**を明示する
- **一時保管（スプール退避）中の表示**: state-warning の色 + アイコン + 補足文言（「ログの保存が追いついていません。一時保管領域に退避しています」）を**セットで常時表示**する——「一時保管」という言葉の無害さを色と文言で補う。**進行中でなくなったら（消化完了。§5.1）静かに消す**——常時表示は「今まさに退避が進行中」の間だけに限り、消化完了後は通知自体を出さない（過去に退避があった事実は現在値カードの累計 + 補足文言で示す。Issue #132）
- **昇格案内**（architecture.md §5.3 の文言の引き受け。文言例は表示面ごとに分ける）: 閲覧画面（ダッシュボード等）の通知は操作を含めない——「ログの保存が受信に追いついていません。保存先を SQL Server に切り替えることで改善が見込めます（切り替えと案内の抑制はサーバ上の設定画面から行えます）」。設定画面（管理リスナ）側では操作ボタンを含む——「…改善が見込めます。［切り替えの手順を見る］［今後表示しない］」。**抑制（今後表示しない）と解除は設定画面（管理リスナ）でのみ確定する**——閲覧画面の通知にはサーバ状態を変更する操作を置かない（§4 の不変条件を文言例のレベルでも守る）
- **切替（昇格）確定前の予告**（database.md §6.1 の文言の引き受け。文言例）: 「切り替えると、これまでに保存したログは移行機能の提供まで画面から参照できなくなります。あとで参照する可能性がある場合は『退避』を選んでください」
- Express 上限接近（database.md §5.3）は state-warning の通知として表示し、利用者の言葉 + 次に取る手順で書く（§7）
- 実装参照（M8-2）: `YaguraSystemNotice`（警告 = state-warning / 案内 = state-info。色 + アイコン + 補足文言のセット表示。**操作ボタンを持たない**——閲覧画面の通知にサーバ状態を変更する操作を置かない、の構造化）。一時保管・昇格案内（閲覧面）の確定文言は `UiText` に収載。操作を含む管理リスナ側の表示面は設定画面（M8-4）の管轄

### 5.5 OS 受信破棄（OS 統計）の扱い（M8-3 で確定。[ADR-0016](../adr/0016-os-drop-gauge-mechanism.md) で計器撤去）

architecture.md §4.2 の M8 必須要求（「UI に表示する際は…常時可視の注記」と「表示自体の要否の判断」）の引き受けと判断記録:

- **判断: 値は表示しない。状態画面に常時可視の説明のみを掲示する**
- **理由**: M7-2 のクロスマシン実機検証（architecture.md §4.2「覆域の限界」）で、検証環境（Windows 10.0.26200）では本 OS 統計が**受信・破棄のいずれも一切計上しない**ことが実測確定した。常に 0 のままの数値を画面に置くと、注記を添えても「取りこぼしゼロ」という誤読（D-6 が明示的に禁じる解釈）の源になる——本設計は誤解を生まない側（非表示 + 説明）に倒す。なお計器（旧 `yagura.os.udp.*`）自体もその後 ADR-0016 決定 3 で**製品コードから撤去された**（本判断の「値を表示しない」は「計器が存在しない」へ強まった）。**説明の掲示は計器撤去後も運用者の導線として維持する**——「OS レベルのロスが画面に無いのは正常」という疑問への答えは、計器の有無と独立に必要であるため
- **常時掲示する説明の確定文言**（状態画面のカウンタ一覧直下。`UiText.OsUdpDiscardExplanation` / 同 `Supplement`）:
  - 本文: 「OS がこのアプリへ渡す前に破棄した受信データの数（OS の統計値）は、この画面に表示していません」
  - 補足: 「検証済みの Windows 環境では、この OS 統計は受信・破棄のどちらも計上しないことが実測で確認されています。0 という値を表示すると『取りこぼしなし』という誤解を生むため、値の表示自体を行いません。取りこぼしの確認には、上記のカウンタと、ダッシュボードの送信元別の受信状況（最終受信時刻）をあわせて確認してください」
- **再評価の条件**: 本 OS 統計が実際に計上する Windows 版・環境が確認された場合（[ADR-0016](../adr/0016-os-drop-gauge-mechanism.md) 再評価トリガ (d)——M9 lab での独立読み取りによる実測）、同 ADR の amendment による**計器の再導入とあわせて**「値の表示 + 常時注記（システム全体値・同居プロセス分の混入）」への切り替えを再検討する。その場合も D-6 の解釈ガイド（値 0 ≠ 取りこぼしゼロ）を注記に含めることが条件である

## 6. 時刻表示の契約

- 保存は UTC（database.md §2.2）。**表示はすべてサーバの OS タイムゾーンでローカル化し、タイムゾーンを画面に明示する**（例: `2026-07-04 21:05:11 (UTC+09:00)`）。オフセット表記は**そのレコードの時刻時点のオフセット**とする（夏時間のあるタイムゾーンでも一意に読める）
- **閲覧者ごとのタイムゾーン変換はしない**。主理由: ①閲覧リスナは認証なしで閲覧者の概念がなく、閲覧者ごとの設定を保持する場所がない（ADR-0004 決定 2 の帰結）、②ターゲットの中心は単一タイムゾーンの環境（ADR-0001）、③表示の一意性は実装も検証も単純にする。補助的に、同じ画面を見る管理者同士で時刻の読みが一致する（口頭・電話での突合が単純になる）。**リモート拠点からの閲覧で閲覧者別表示の要望が反復した場合（目安: 独立した利用者から 3 件以上）に再評価する**
- 基準軸はサーバ受信時刻（ReceivedAt）。送信元が名乗った時刻（DeviceTimestamp）は詳細表示で併記し、受信時刻との乖離が閾値（仮値: 5 分。UI-2）以上の場合は乖離量と向き（進み / 遅れ）を注記する（装置の時計ずれの発見に使う。実装: `LogSearch.razor` 詳細表示の `YaguraSystemNotice`）。**RFC 3164（タイムゾーン情報なし）由来の DeviceTimestamp は解析時にタイムゾーンを UTC とみなす近似（database.md §2.1）であるため、この乖離は真の時計ずれだけでなく送信元のタイムゾーン設定の違いを表すこともある**。レコードから RFC バージョンを判別できる永続化済み情報がなく個別の出し分けができないため、注記は常に同じ規則（単純な差分 + 閾値）で出し、この限界を注記の補足文言（`UiText.DetailDeviceTimestampDriftSupplement`）で明示する（2026-07-09 判断。Issue #158）
- **契約の実装位置（M8-2）**: 整形は `YaguraTimeFormatter`（`src/Yagura.Web/Components/Common/`）の 1 箇所に集約し、画面の時刻表示は `YaguraTimestamp` コンポーネント経由に統一する（`<time>` の datetime 属性に ISO 8601 の UTC 表記を併せて出す——機械可読の一意表現）。画面が独自の書式・変換を持つことを禁止する
  - **日付だけを見せたい範囲**（証明書の有効期間等）も契約の側で扱う: `YaguraTimeFormatter.FormatLocalDateRange`（例 `2026-07-04 〜 2031-07-04 (UTC+09:00)`）。日付のみでもタイムゾーンの明示は要る——同じ瞬間でもタイムゾーンによって日付が 1 日ずれるため。オフセットは範囲の末尾に 1 つだけ添える（夏時間を跨ぐ場合は終端側を採る——有効期限の判断は終端を見る操作であるため）
  - **禁止は走査で強制する**（Issue #462）: `TimeFormatContractTests` が `.razor` を横断走査し、日付・時刻のカスタム書式を伴う `ToString` と、画面での `ToLocalTime()` / `ToUniversalTime()` を検出したら落ちる。**往復（ラウンドトリップ）書式は対象外**（`"o"` / `"O"` / `"s"` / `"u"`）——本節が縛るのは表示であり、機械可読の直列化は別物である（`LogSearch` は検索条件の期間を `"O"` で URL へ載せる。サーバのタイムゾーンに依存せず、共有された URL がどの端末で開かれても同じ瞬間を指すための意図的な選択）
  - **経緯**: 本規約は明文だったが人手のレビューだけに委ねており、画面が増えるたびに独自整形が 1 つずつ足されて **7 画面**がドリフトしていた（Issue #462）。最も重かったのはフォワーダキット画面のアカウント点検で、最終ログインを **UTC 生表記**で出していた——JST の管理者には 9 時間前の出来事に見え、「心当たりのないアカウントか」を判断する材料が判断を誤らせていた

## 7. 用語対応表と文言原則

### 7.1 文言原則（ADR-0003 決定 5）

- 画面の文言は平易な日本語とし、開発用語を利用者向けの画面に出さない。エラーメッセージ・通知（イベントログ警告の本文を含む）も対象
- **対応は 1 対 1 とする**: 画面の言葉 1 つは開発用語 1 つに一意対応させ、複数の開発用語を同じ平易語に写像しない（試用報告・監査報告が設計文書と突合できることを対応表の設計要件とする）
- 文言はリソースとして分離する（将来の英語化に備える。v0.1 の提供言語は日本語のみ）。実装参照（M8-2）: 共通コンポーネントの文言は `UiText`（文言カタログ。部品は文言リテラルを持たない）に分離済み——v0.1 は日本語のみのため静的カタログとし、英語化時に .resx 移行の単一起点とする
- **新しい開発用語を画面に導入する PR は、本表への追記を同じ PR に含める**。利用者向けヘルプ・マニュアル（operations.md 系）も本表を参照する。**syslog プロトコル用語（severity・facility 等）も本表の対象**であり、実装設計で画面に出す用語から順次追記する

### 7.2 用語対応表（初版）

| 開発用語（設計文書） | 画面の言葉 |
|---|---|
| スプール / スプール退避 | 一時保管 / 一時保管への退避（表示規約は §5.4——色・アイコン・補足文言をセットで） |
| ドロップ / 破棄 | 取りこぼし（**サーバに届いた後に失われたログを指す。ネットワーク上のロス——サーバが観測できない——はこの言葉に含めない**。architecture.md D-2 と同じ限界を画面の言葉にも適用する） |
| バックプレッシャ | （画面に出さない——挙動の説明は「受信量が処理を上回っています」） |
| provider | 保存先（データベース） |
| 本番昇格 | 保存先を SQL Server に切り替える |
| opt-in / opt-out | 追加で有効にする / 標準で有効（無効にできます） |
| 保持期間 | ログを保存しておく期間 |
| 受信断 | 受信できなかった時間帯 |
| 縮退運転 | 一部機能を停止して動作中 |
| 解析失敗（raw 保存） | 形式を解釈できなかったログ（原文のまま保存しています） |
| circuit / 再接続 | 画面とサーバの接続 / 再接続 |
| severity | 重大度 |
| facility | 分類（ファシリティ） |
| 無音化（UI-4） | 最後に受信してからの経過時間（が長いこと） |
| 不完全（TCP 途中終端。ParseStatus） | 切断により途中で途切れたログ（原文のまま保存しています） |
| 解析済み（ParseStatus.Parsed） | 解析済み（検索条件の解析状態欄。Issue #148） |
| 内部バッファ破棄（`yagura.ingestion.internal_buffer.dropped`） | 取りこぼし（サーバ内の処理待ちが満杯） |
| TCP 接続拒否（`yagura.ingestion.tcp_connection.rejected`） | 同時接続の上限で受け付けなかった接続 |
| TCP / TLS 接続の異常終了（`yagura.ingestion.tcp_connection.faulted`。Issue #482） | 取りこぼしの疑い（接続が想定外の理由で終了） |
| スプール退避（`yagura.ingestion.spool.evacuated`） | 一時保管への退避（取りこぼしではありません） |
| スプール書込失敗（`yagura.ingestion.spool.write_failed`） | 取りこぼし（一時保管への保存失敗） |
| スプール破棄（`yagura.ingestion.spool.discarded`） | 取りこぼし（一時保管が満杯） |
| スプール末尾破損破棄（`yagura.ingestion.spool.corrupt_tail_discarded_bytes`） | 取りこぼし（一時保管ファイルの末尾破損。単位はバイト） |
| 永続化失敗（`yagura.ingestion.persistence.failed`） | 取りこぼし（保存の失敗） |
| 流量制御破棄（`yagura.ingestion.flow_control.dropped`） | 取りこぼし（送信元ごとの受信量の制限） |
| TLS ハンドシェイク失敗（`yagura.ingestion.tcp.tls_handshake_failure`。Issue #509） | TLS の接続確立に失敗した回数（送信側との不一致）——**診断用の区画に出す。再起動でリセットされる** |
| UDP 受信エラー（`yagura.ingestion.udp.receive_error`。Issue #509） | UDP の受信処理でエラーになった回数（同上） |
| システムイベント Kind: downtime.normal-stop / downtime.crash-approximate / downtime.listener-reconfigure / downtime.listener-bind-retry | 受信できなかった時間帯（停止・再起動による / 正常に終了しなかったため境界はおおよそ / 設定反映（リスナ再構成）による——Issue #262 / ポートを開けなかった間（再試行で復旧）——Issue #291） |
| システムイベント Kind: retention.delete | 古いログの自動削除を実行 |
| Windows 統合認証（Integrated Security） | Windows 統合認証（サービスのアカウントで接続） |
| SQL Server 認証（SQL 認証） | SQL Server 認証（ユーザー名とパスワードで接続） |
| TrustServerCertificate | サーバ証明書を信頼する（証明書の検証を省略。通信の暗号化は維持。なりすまし検知は行われない——自己署名証明書の閉域環境向け） |
| 修復 SQL（database.md §5.2 の提示 SQL） | 解決するための SQL（このサーバは実行しません。管理者が SQL Server 側で実行してください） |
| syslog HOSTNAME（送信者の自己申告） | ホスト名（ログに記載） |
| 逆引き（PTR）ホスト名 | 逆引きホスト名（DNS に登録された名前。ADR-0007——ログに記載のホスト名とは別由来。画面のツールチップ確定文言は §4 を正とする） |
| 逆引き解決 成功（`yagura.web.reverse_dns.resolved`） | 逆引きホスト名の取得（成功） |
| 逆引き解決 未登録（`yagura.web.reverse_dns.not_found`） | 逆引きホスト名の取得（名前の登録なし——異常ではありません） |
| 逆引き解決 失敗（`yagura.web.reverse_dns.failed`） | 逆引きホスト名の取得（失敗——DNS の応答なし・エラー） |
| 逆引き解決 見送り（`yagura.web.reverse_dns.skipped`） | 逆引きホスト名の取得（見送り——キャッシュが満杯） |

- カウンタ 8 種の平易語は**状態画面で識別子（計器名）と併記**する（§4 の「報告と設計文書を機械的に突合できるようにする」の表示面。1 対 1 対応——複数の計器を同じ平易語に写像しない。「取りこぼし（…）」の括弧内が計器ごとの識別を担う）。スプール末尾破損破棄（Issue #201）のみ単位がバイトのため、平易語のラベル自体に「単位はバイト」を含めて他行との桁の不一致による誤解を防ぐ

### 7.3 再接続まわりの文言例（ADR-0003 / PR #5 佐藤の委任）

- 再接続中: 「サーバとの接続が一時的に切れました。自動的に再接続しています——ログの受信は継続しています」（**受信は止まっていないことを必ず添える**——閲覧の切断とログの喪失を混同させない）
- 再接続失敗: 「接続を回復できませんでした。ページを再読み込みしてください。表示が古い可能性があります（最終更新: ◯◯時◯◯分）」

## 8. アクセシビリティ基準（ADR-0003 決定 6 の個別化）

**ライト・ダーク両テーマで**次を受け入れ条件とする:

- **キーボード完遂**: 検索の実行・結果の閲覧・詳細表示・設定変更・ウィザード完了までの主要操作が、マウスなしで完遂できる。フォーカスは常に視認できる（フォーカスリングを消さない）。ダイアログは Esc で閉じられ、フォーカスが呼び出し元に戻る
- **コントラスト**: テキストと背景のコントラスト比は WCAG 2 の AA 基準値（通常テキスト 4.5:1 以上・大きなテキスト 3:1 以上）を満たす。状態色のテキスト利用時も同様。§2.1 の初期値はこの基準での検証を経て確定する（UI-1。未達の色はトークン側を調整する——トークン経由の強制（§1）が調整の影響を局所化する）
- **色以外の手がかり**: 状態・エラーは色 + アイコンまたは文言で伝える（§2.1）
- 検証はテーマごとに行い、結果（検証日・ツール・対象トークン）を本文書に記録する

**コントラスト検証記録（UI-1。検証日 2026-07-06。M8-2 / Issue #69）**:

- ツール: リポジトリ内テスト `DesignTokenContrastTests`（tests/Yagura.Web.Tests/Theming/。WCAG 2 の相対輝度式による機械計算。検証対象は実装のテーマ定義 `YaguraTheme.Instance` そのもので、**毎ビルド自動実行される**——以後のトークン変更はこのテストが恒常的に検証する）
- 対象: 両テーマの全実利用ペア——本文・補助テキスト・primary（リンク）・状態色 4 種のテキスト利用 × 背景/面、ContrastText × primary/状態色の塗り面、アプリバー/ドロワーの文字 × 面（計 21 ペア/テーマ）
- 結果: **ライトの state-warning 初期値 #B26A00（対 surface 4.24 / 対 background 3.95）と state-info 初期値 #0277BD（対 background 4.47）が 4.5:1 未達**だったため、UI-1 の手続きどおりトークン側を調整した——state-warning #A05F00（対 surface 5.08 / 対 background 4.74 / 白文字対塗り 5.08）・state-info #0270B2（対 surface 5.30 / 対 background 4.94 / 白文字対塗り 5.30）。他のライト全ペアとダーク全ペアは調整なしで基準を満たした
- 大きなテキスト（3:1）の緩和は使っていない——全ペアを通常テキスト基準（4.5:1）で判定した

## 9. UI メトリクス（circuit・再接続）

（PR #5 鈴木 / ADR-0004 決定 6 の委任）

- 次を architecture.md §4.6 のゲージ・カウンタ体系に加える（同節にも記載済み）: **circuit 現在数（対上限比）** / **再接続の発生回数** / **再接続失敗（circuit 喪失）回数**
- 上限の値・到達時挙動・認証失効の反映はセキュリティ設計文書の管轄。本文書は「上限に対する現在値が観測できること」を UI 側の要件として引き受ける（状態画面 §4 に表示）
- **逆引きホスト名解決のカウンタ（ADR-0007 決定 6）**: 解決の**成功 / 未登録（PTR なし）/ 失敗（打ち切り・DNS 障害）/ 見送り（キャッシュ上限による解決見送り）**を発生箇所別カウンタとして観測する（DNS インフラの異常が「名前が出ない」という見た目の変化だけで黙って進行しないため——未登録は正常系、失敗の増加が異常のシグナル、見送りの増加はキャッシュ運用の逼迫のシグナル）。計器名は architecture.md §4.1.1 の規則（意味論の異なる事象ごとに独立した計器名）に従い `yagura.web.reverse_dns.resolved` / `.not_found` / `.failed` / `.skipped` の 4 種を予定し、確定名は実装 PR で同節の一覧へ追記する。**能動通知（architecture.md §4.6）の対象には加えない**——同通知の対象は「ログを失う・失いかけている事象」に限定しており、本機能の失敗は参考表示の劣化であってログの完全性に影響しない（ADR-0007 決定 6 の判断記録。状態画面のカウンタ一覧で確認できる）

## 10. MudBlazor の統治

- 採用と選定理由は ADR-0003 決定 3（バージョンは導入 PR 時に当日ライブ検証し、`Vendored:` 記録を残す——conventions.md）
- **同梱静的アセットの脆弱性監視の具体手段**（ADR-0003 決定 3 の委任）:
  1. **NuGet パッケージとしての監視**: CI の依存脆弱性スキャン（conventions.md 既定）と GitHub の依存アラートで MudBlazor 本体の既知脆弱性を検出し、検出時は PR をブロックする。**PR が立たない保守期の検知は、下記 2 の定期確認が担う**
  2. **公式セキュリティ勧告の追従**: MudBlazor の GitHub Security Advisories・リリースノートを定期確認する。頻度と実行方式（依存更新の定期 PR）は [UI-5](#12-確定待ち一覧)。**確定までの暫定運用として、GitHub 依存アラートとリリースの確認を月次で行う**
  3. **同梱アセットの実体確認**（UI-3。導入 PR（M8-1 / Issue #68）で初回実施済み。**MudBlazor の版を更新する PR は同じ①〜③を再実施して本節の記録を更新する**）。確認項目を固定する: ①同梱 CSS/JS 内の第三者ライブラリの混入有無（ライセンス表記・著作権表示の走査）、②**外部 URL 参照の有無**（`@import`・`@font-face` の外部参照・source map・動的ロード——§2.2「Web フォントを参照しない」の受け入れ検証を兼ねる。**検出した場合は該当参照を除去またはトークン/テーマ上書きで到達させない処置を行う**）、③確認した版・確認日・確認方法の記録

  **初回実施の記録（MudBlazor 9.6.0。確認日 2026-07-06）**:

  - 対象: NuGet パッケージ mudblazor/9.6.0 の staticwebassets 配下の全 3 ファイル——MudBlazor.min.css（609,235 バイト）/ MudBlazor.min.js（70,193 バイト）/ MudBlazor.min.js.map（254,124 バイト）。実サーバからの配信バイト数がパッケージ実ファイルと一致することも確認した
  - **① 第三者ライブラリの混入: なし**。ライセンス・著作権表記の走査で検出されたのは MudBlazor 自身の MIT ヘッダ（Copyright (c) 2021 MudBlazor）のみ（normalize.css 等の第三者表記なし）
  - **② 外部 URL 参照: なし（除去・遮断の処置は不要）**。CSS: `@import` 0 件・`@font-face` 0 件・`url()` 参照は `data:` URI（埋め込み PNG）1 種のみ・sourceMappingURL なし・フォント名のハードコード 0 件（Roboto を含む。フォントはすべてテーマ由来の CSS 変数参照——§2.2）。JS: 動的ロード・外部通信の構文（`import()` / `fetch()` / XMLHttpRequest / WebSocket / script 要素生成）0 件。JS 末尾の `sourceMappingURL` は同梱 MudBlazor.min.js.map への**相対参照**（同一オリジン配信であり外部到達なし。map の中身は MIT 公開済みソースのため配信を許容する）。CSS/JS 内の `https://` 文字列はライセンスコメント内の表記のみで、到達コードではない
  - **③ 確認方法**: NuGet キャッシュの実ファイルへの機械走査（`@import` / `@font-face` / `url(` / `https?://` / `sourceMappingURL` / 動的ロード構文 / copyright・license 表記のパターン検索）。配信面の検証は L-5 アーキテクチャテスト（`ViewerEndpointAllowlistTests`）が恒常化した——静的アセット経路が「`_content/MudBlazor/` 配下の既知 3 アセット（+フィンガープリント・圧縮変種）のみ・GET/HEAD のみ」であることを毎ビルド検証する（M8-2 / Issue #69 で Yagura.Web 自前アセット 2 ファイル——共通コンポーネント CSS `yagura-components.css` と §5.2 のステール警告 JS `stale-guard.js`——が同型の制約付きパターンとして加わった。security.md §1 も同旨）。また .NET 10 の MapStaticAssets が build マニフェスト使用時に追加する「マニフェスト外ファイルの開発時フォールバック配信」は設定キー `DisableStaticAssetNotFoundRuntimeFallback=true` で無効化し、配信面をマニフェスト記載のアセットに固定した（Yagura.Host/Program.cs）
- メジャー更新の追従・保守停滞時の再評価は ADR-0003 の再評価トリガに従う。ページが MudBlazor API を直接使う面を §1 の規約で狭く保つことが、更新・乗り換えの影響範囲を限定する（同 ADR の緩和策の実行形）

## 11. ページ固有 CSS 例外一覧

§1 の例外手続きで認めた項目の追跡台帳（現在: なし。§5.2 のステール警告 JS は本文書で設計確定した例外であり、本台帳の対象外）。

| # | 対象ページ | 内容 | 理由 | 登録 PR | 共通化の見込み |
|---|---|---|---|---|---|
| — | — | — | — | — | — |

## 12. 確定待ち一覧

architecture.md §9 と同じ運用。番号は UI-x。

| # | 項目 | 決め方 | 反映先 |
|---|---|---|---|
| UI-1 | ~~配色トークンのコントラスト検証（両テーマ・全状態色）~~ → **✅ 確定 → §2.1・§8**（M8-2 / Issue #69。リポジトリ内テスト `DesignTokenContrastTests` で機械検証を恒常化。ライトの state-warning / state-info を調整——検証記録は §8） | 実装期にコントラスト検証ツールで確認し、未達はトークン値を調整して本文書を更新（検証日・ツールを記録） | §2.1・§8 |
| UI-2 | ダッシュボードの更新間隔・ステール警告の閾値（仮値: 更新間隔の 3 倍）・DeviceTimestamp 乖離表示の閾値（仮値: 5 分） | 仮値で実装し、実利用で確定 | §5.2・§6 |
| UI-3 | ~~MudBlazor 同梱アセットの実体確認（確認項目は §10 の①〜③に固定）~~ → **✅ 確定 → §10**（M8-1 / Issue #68。MudBlazor 9.6.0 で①〜③実施済み・外部参照なし。版更新 PR での再実施規約も §10 に記載） | 導入 PR で確認し、結果と（必要なら）追加の監視手段を §10 に記録 | §10・§2.2 |
| UI-4 | ~~送信元別受信統計の表示粒度~~ → **✅ 確定 → §4**（M8-3 / Issue #70。送信元アドレス単位・最終受信時刻の古い順の全送信元一覧・上限 1,000 + ページング 20 件/ページ。集計は provider 側 `QuerySourceActivityAsync`——上限打ち切りは「最近まで受信できている送信元」側に効き、無音化検出を損なわない。制約「量の上位 N のみの表示は不可」を満たす。**宛先アドレス別の統計は見送りのまま**——要望が反復したら再評価） | ダッシュボード実装設計で確定。**制約: 量の上位 N のみの表示は不可**——無音化した送信元の検出（最終受信時刻の古い順の全送信元一覧。閲覧はページングで防御）を必須要件とする（「いつも静かな装置が黙った」は上位 N に原理的に現れない） | §4・§5.1 |
| UI-5 | 依存更新の定期 PR の頻度・実行方式（§10 の 2 段目の実行媒体） | 実装期に確定。**確定までの暫定運用: 依存アラート・リリースの月次確認** | §10 |
| UI-6 | 状態帯の判定観測窓の幅・復帰条件・確認済み操作の要否。**一時保管への退避の復帰条件のみ → ✅ 一部確定**（M8-3 追補 / Issue #132。観測窓ではなくスプールの現在ゲージ `CurrentUsageBytes == 0`＝消化完了で判定。フラップ対策の追加窓は設けない——退避の正シグナルが直接使えるため待つ理由がなく、瞬時の復帰そのものが正しい表示という判断）。**残り（観測窓 5 分の幅・取りこぼしの復帰条件・確認済み操作の要否・受信リスナ判定）は仮値のまま** | 仮値で実装し、フラップ（点滅の摩耗）と見逃しの両方を実利用で確認して確定 | §5.1 |
| UI-7 | 試用報告様式の項目一覧（バージョン・provider・稼働期間・カウンタ・OS 統計突合値・受信断有無・取得時刻）とコピー形式 | operations.md 起筆時（試用協力の呼びかけ文書と合同）で確定。**UI 停止時の代替取得経路（メタデータ領域の直接参照可否）も同時に定める** | §4 |
| UI-8 | ~~トークンと MudBlazor テーマ定義の具体割当~~ → **✅ 確定 → §2.1.1**（M8-1 / Issue #68。実装は YaguraTheme.cs、描画 CSS 変数の実機確認済み） | 導入 PR で確定し §2.1 に記録（本文書が正。同期規約は §2.1） | §2.1 |
| UI-9 | ~~導入 PR での実体検証群: フォントスタックの実在と表示品質（非 Windows クライアントのフォールバック確認を含む）・影 3 段の elevation 割当・OS テーマ追従の JS 非依存実現可否~~ → **✅ 確定 → §2.2（フォント実在・サイズ割当）・§2.3（elevation 割当・角丸 2 値の方式）・§2.4（OS 追従: JS 手書きなしで可）**（M8-1 / Issue #68。再設計が要ったのは角丸 2 値のみ——テーマは 1 値で、8px はカード/ダイアログ共通コンポーネント内部で適用） | 導入 PR で検証し、結果を各節に記録（不可の項目は方式を再設計） | §2.2・§2.3・§2.4 |
| UI-10 | 逆引きホスト名解決の数値群（成功の保持時間・未登録の保持時間・同時解決数の上限・キャッシュ件数の上限・解決待ちの打ち切り時間・表示反映の束ね周期）※機構自体は ADR-0007 決定 2・3 で確定済みで、本項の対象は数値のみ。**仮値で実装済み**（`ReverseDnsResolverLimits.Default` = 成功 30 分・未登録/失敗 5 分・同時解決 4・キャッシュ 10,000 件・打ち切り 10 秒・束ね 500 ms。机上試算: 送信元 1,000 件 × PTR 整備率 50% の初回表示でも、ミスは描画中ページ分に限られ同時 4 に絞られる——最悪でも DNS へは逐次 4 本。**同時上限到達時の扱いは「延期」で確定**——キューに積まず次の表示更新で再試行・計上なし。キャッシュ上限の「見送り」= skipped 計上とは区別） | 仮値で実装済み（値は左欄）。実利用——PTR 未整備環境の体感・DNS サーバへの負荷——を踏まえて確定。**DNS サーバ無応答時に閲覧層のタスク・スレッドを専有し続けないこと（ADR-0007 決定 3 の間接影響シナリオ）を確定時の検証項目に含める** | §4 |
