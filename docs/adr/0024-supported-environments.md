# ADR-0024: 対応環境と推奨環境の定義 — v1.0 で表明するサポート行列

- 状態: accepted（2026-08-01 マージ）
- 日付: 2026-07-26
- 決定者: YANAI Taketo
- 関連: [ADR-0001](0001-project-founding.md)（目的・対象利用者・導入体験の原則）/ [ADR-0004](0004-security-model.md) 決定 2（Server Core 等での管理手段）・決定 3（opt-in 強化 3 点）/ [ADR-0006](0006-v1-release-criteria.md)（v1.0 公開基準 1・3・4）/ [ADR-0009](0009-architecture-support.md)（アーキテクチャ対応。本 ADR は決定 2 の x64 行を部分的に supersede する）/ [ADR-0022](0022-viewer-https.md)（閲覧 HTTPS）/ [ADR-0023](0023-upgrade-availability.md)（アップグレード時の可用性）/ [architecture.md](../design/architecture.md) §5.2・§9 M-6 / [database.md](../design/database.md) §5.3・§5.4 / [Issue #482](https://github.com/Yanai-Taketo/Yagura/issues/482)（TLS 接続ハンドラの無音脱落。本 ADR の起案中に発見）/ [PR #481](https://github.com/Yanai-Taketo/Yagura/pull/481)（起案・ペルソナレビュー） / **未了の委任事項の追跡**: [#494](https://github.com/Yanai-Taketo/Yagura/issues/494)（委任 1）・[#495](https://github.com/Yanai-Taketo/Yagura/issues/495)（委任 2）・[#496](https://github.com/Yanai-Taketo/Yagura/issues/496)（委任 6）・[#497](https://github.com/Yanai-Taketo/Yagura/issues/497)（委任 5・10）

## 文脈と課題

v1.0 は利用者に対する「本番利用を推奨できる」という宣言である（ADR-0006）。しかし現時点の Yagura は、**動作環境を利用者に一切表明していない**。README にあるのは ADR-0009 が定めた対応アーキテクチャ表（x64 = 対応 / ARM64 = 試験的）だけで、**OS のバージョン・SQL Server のバージョン・ブラウザ・ハードウェアの要件はどの文書にも存在しない**。

ADR-0009 委任事項 7 は「README のシステム要件表をアーキ別に更新する」と書いたが、実際にはシステム要件表そのものが存在せず、アーキ表だけが作られた。更新すべき土台が無かったためである。

これは ADR-0006 基準 3（利用者向け文書の完備——作者と独立した人間が文書だけを頼りに導入から運用まで到達できること）の穴である。導入判断の最初の一歩、「うちの Windows Server で動くのか」に答える文書が無い。

さらに問題は表明の欠落だけではない。**v0.x の間に実機で踏んだ環境は狭い**。「表明していない」だけでなく「検証していない」状態であり、v1.0 で何を約束するかを決めるには、まず外部の前提（.NET・Windows・SQL Server・ブラウザのサポート状況）を確定させる必要がある。

### 実機検証の現況（v0.x で実際に踏んだ環境）

| 環境 | 検証実績 | 出典 |
|---|---|---|
| Windows Server 2025（10.0.26100） | gMSA・ACL/監査・通知/無音化・起動警告・MSI アップグレードの各 lab | `installer/lab/` の各手順書 |
| Windows 11 / Windows 10 Pro クライアント | 開発機ベンチ・lab メンバー機 | `tools/Yagura.Bench/results/` |
| Windows Server 2022 | CI ランナー（当時の `windows-latest`）でのビルド・単体テスト・回帰ベンチ | `.github/workflows/ci.yml` |
| Windows 11 on Arm（ARM64） | ADR-0009 Phase 0/1 のスモーク + リリースごとのスモーク | ADR-0009・`release.yml` |
| SQL Server 2022 Express | M7-2 実測・全 lab の保存先 | `tools/Yagura.Bench/results/2026-07-05-yagura-stg/` |
| SQL Server LocalDB | **全 PR の CI で適合テストが実走**（下記「CI の現況」） | `.github/workflows/ci.yml` |
| **Windows Server 2019 / 2016** | **なし** | — |
| **SQL Server 2019 以前 / 2025 / Express 以外のエディション** | **なし** | — |

現行 CI の `windows-latest` は Server 2025 を指す（[GitHub-hosted runners reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners) の「`windows-latest` (Windows 2025)」。確認日 2026-07-26）ため、**Server 2022 は現在どのジョブでも踏まれていない**。

**検証実績の限界**: 上表の「Windows 10 Pro」は、後述の外部検証 (1) が示すとおり **.NET 10 の公式サポート対象外のエディション**である（.NET 10 の Windows 10 対応は LTSC・Enterprise に限られる）。この実績を対応表明の裏付けには使えない。

### 外部前提のライブ検証（2026-07-26 実施）

conventions.md の実体検証原則に従い、本 ADR の判断根拠となる外部サポート状況を当日ライブ確認した。

**(1) .NET 10 の Windows 対応**（[Install .NET on Windows](https://learn.microsoft.com/en-us/dotnet/core/install/windows)、更新日 2026-06-02、確認日 2026-07-26）: Windows Server 系列（2012〜2025・Server Core・Nano Server）は **x64 のみ**。クライアントは **Windows 11** と **Windows 10（21H2 / 1809 / 1607）** で、同ページは「**Windows 10 support is limited to Long-Term Servicing Channel (LTSC) and Enterprise editions only.**」と明記する——すなわち **Windows 10 の Home / Pro は .NET 10 の対象外**である。また **Windows Server 2012 / 2012 R2 は「Microsoft Visual C++ 2015-2019 再頒布可能パッケージ」を前提条件**として明記している。

**(2) .NET 10 のライフサイクル**（[.NET Support Policy](https://dotnet.microsoft.com/en-us/platform/support/policy/dotnet-core)、確認日 2026-07-26）: .NET 10 は **LTS、サポート終了 2028-11-14**。

**(3) Schannel の TLS 1.3 対応**（[Protocols in TLS/SSL (Schannel SSP)](https://learn.microsoft.com/en-us/windows/win32/secauthn/protocols-in-tls-ssl--schannel-ssp-)、更新日 2025-03-20、確認日 2026-07-26）: 該当箇所を引用する——「**TLS 1.3 is supported starting in Windows 11 and Windows Server 2022. Enabling TLS 1.3 on earlier versions of Windows is not a safe system configuration.**」。同ページの表でも Windows Server 2019・2016 の TLS 1.3 は Client/Server とも `Not supported`。**なお本 ADR は暗号スイートの差異までは調査していない**（決定 1 の委任事項 1 (iv) で扱う）。

**(4) Blazor の対応ブラウザ**（[ASP.NET Core Blazor supported platforms](https://learn.microsoft.com/en-us/aspnet/core/blazor/supported-platforms?view=aspnetcore-10.0)、更新日 2026-07-22、確認日 2026-07-26）: Apple Safari・Google Chrome・Microsoft Edge・Mozilla Firefox の **Current（最新版）のみ**。Internet Explorer は対応表から外れている（ASP.NET Core 3.1 版の同ページには `Microsoft Internet Explorer | Not Supported` と明示されていた）。

**(5) Windows Server の同梱ブラウザ**（[What's New in Windows Server 2022](https://learn.microsoft.com/en-us/windows-server/get-started/whats-new-in-windows-server-2022)、更新日 2026-02-16、確認日 2026-07-26）: 該当箇所を引用する——「**Microsoft Edge is included with Windows Server 2022, replacing Internet Explorer.** (中略) It can be used with the **Server with Desktop Experience** installation options.」。すなわち **Server 2022 以降は Edge が同梱される**が、**それ以前の Server では同梱されず**、**Desktop Experience 以外の導入形態（Server Core）では使えない**。Server 2019 / 2016 の素の状態での実挙動は委任事項 1 項目③で確認する。なお同ページは「**HTTPS and TLS 1.3 is now enabled by default on Windows Server 2022**」とも述べており、外部検証 (3) と整合する。

**(6) OS・DB のライフサイクル**（Microsoft Lifecycle の各製品ページ、確認日 2026-07-26）:

| 製品 | メインストリーム終了 | 延長サポート / 提供終了 |
|---|---|---|
| [Windows Server 2016](https://learn.microsoft.com/en-us/lifecycle/products/windows-server-2016) | 2022-01-11 | **2027-01-12** |
| [Windows Server 2019](https://learn.microsoft.com/en-us/lifecycle/products/windows-server-2019) | 2024-01-09 | **2029-01-09** |
| [Windows Server 2022](https://learn.microsoft.com/en-us/lifecycle/products/windows-server-2022) | 2026-10-13 | **2031-10-14** |
| [Windows Server 2025](https://learn.microsoft.com/en-us/lifecycle/products/windows-server-2025) | 2029-11-13 | **2034-11-14** |
| [Windows 10 Home / Pro](https://learn.microsoft.com/en-us/lifecycle/products/windows-10-home-and-pro) | — | **2025-10-14（終了済み）** |
| [SQL Server 2016](https://learn.microsoft.com/en-us/lifecycle/products/sql-server-2016) | 2021-07-13 | **2026-07-14（終了済み）** |
| [SQL Server 2017](https://learn.microsoft.com/en-us/lifecycle/products/sql-server-2017) | 2022-10-11 | **2027-10-12** |
| [SQL Server 2019](https://learn.microsoft.com/en-us/lifecycle/products/sql-server-2019) | 2025-02-28 | **2030-01-08** |
| [SQL Server 2022](https://learn.microsoft.com/en-us/lifecycle/products/sql-server-2022) | 2028-01-11 | **2033-01-11** |
| [SQL Server 2025](https://learn.microsoft.com/en-us/lifecycle/products/sql-server-2025) | 2031-01-06 | **2036-01-06** |

**SQL Server 2016 の延長サポートは本 ADR 起案の 12 日前（2026-07-14）に終了した**。**Windows 10 Home / Pro は 2025-10-14 に提供終了済み**である。

**検証の限界**: Microsoft.Data.SqlClient が**接続先としてサポートする SQL Server のバージョン下限**を明示した公式ページは見つからなかった（README・Learn の名前空間紹介ページとも .NET 側の要件〔.NET 8.0+〕しか記載しない）。したがって本 ADR の SQL Server 下限は、ドライバ側の制約ではなく **Microsoft のライフサイクルと Yagura 自身の検証実績**を根拠に定める。

### CI の現況（本 ADR の決定 5 の前提）

`.github/workflows/ci.yml` の実ファイルを確認した結果は次のとおり（確認日 2026-07-26）。

- **SQL Server 適合テストは全 PR の CI で実走している**。GitHub ランナーの image に SQL Server インスタンスは同梱されないが、`ci.yml` の `Ensure SQL Server LocalDB is available` ステップが公式 `SqlLocalDB.msi` をその場でサイレント導入し、適合テストは `(localdb)\MSSQLLocalDB` を既定接続先として実行される。CI ではスキップせず fail する設計（偽装 green の防止）
- **ただしその実行基盤は LocalDB であり、実インスタンス（Express / Standard / Enterprise）に対する CI 実行は無い**。決定 2 は LocalDB を運用形態としては対応外とするため、**SQL Server provider の常設・blocking な自動検証が「対応外の実行形態」の上にのみ立っている**という捻れが残る。この捻れは決定 2 で明示的に扱う
- 回帰ベンチの blocking 判定は**突合成立（`requireReconciled`）のみ**である（M-5 v2。architecture.md §5.2）。スループット・保存件数の比判定はランナー個体差が支配的で blocking にできないことが実測で確定済み。**性能退行の blocking 検知は基準環境での絶対値判定（M-6）の管轄**
- 基準値ファイル `tools/Yagura.Bench/baselines/ci-baseline.json` は **1 本**で、ランナー OS 別の区分を持たない。その出自は 2026-07-06 の `windows-latest`（当時 = Server 2022）実測であり、**現行の実行環境（Server 2025）とずれている**

### 検証で判明した製品側の未確認事項・欠陥

外部前提の確認と併せてコードを監査し、本 ADR の決定に影響する事項を 3 点特定した。

- **TLS 1.3 の固定指定**: Yagura は TLS を使う 3 か所すべてで `SslProtocols.Tls12 | SslProtocols.Tls13` を固定している（syslog TLS 受信 = `src/Yagura.Ingestion/Tls/TlsSyslogListener.cs:245`、管理 HTTPS・閲覧 HTTPS = `src/Yagura.Host/Program.cs:359`）。**TLS 1.3 非対応の OS でこの指定が TLS 1.2 へ縮退するのか、例外になるのかは未検証**である
- **接続ハンドラの無音脱落（欠陥。[Issue #482](https://github.com/Yanai-Taketo/Yagura/issues/482)）**: 上記の検証中に発見。`TlsSyslogListener.HandleConnectionAsync` の catch フィルタは `AuthenticationException or IOException or OperationCanceledException` に限定され（`TlsSyslogListener.cs:268`）、包括 catch を持たない。接続タスクを起動する `ContinueWith`（同 183〜189 行）も `t.Exception` を観測しない。**フィルタ外の例外型が出ると、カウンタにも警告ログにも残らずに接続が落ちる**——プロセスは生存し TCP accept も継続するため外形監視は緑のまま。ADR-0001 の品質原則「失った場合に必ず観測できる」に反し、**ADR-0006 基準 2 の重大不具合定義①に該当する**（未解決のまま v1.0 は公開できない）。TLS 1.3 非対応 OS はこの経路を踏み得る一例にすぎず、**欠陥そのものは TLS 1.3 と独立**である
- **MSI に OS バージョンの起動条件が無い**（`installer/Package.wxs`）。対応外の OS にもインストールできてしまう

## 検討した選択肢

### 対応 OS の下限をどこに置くか

**識別因子の整理を先に置く。** Server 2016 と Server 2019 を比較すると、**TLS 1.3 非対応（外部検証 (3)）も Edge 非同梱（同 (5)）も両者に等しく当てはまる**。したがってこの 2 点は 2016 と 2019 を分ける根拠として使えない。**両者を実際に識別するのはライフサイクルの残存期間のみ**である（2027-01-12 と 2029-01-09）。この事実を踏まえて選択肢を評価する。

- **(A) .NET 10 が対応する全 OS（Server 2012 以降）を対応と表明する**: 却下。理由は 2 点——①**Server 2012 / 2012 R2 は VC++ 再頒布可能パッケージの別途導入が前提**であり（外部検証 (1)）、ADR-0001 の**ゼロ設定ファーストラン**が成立しない。②Server 2016 の残存ライフサイクルが約 6 か月しかなく、下記のコスト比較が成立しない。**なお「検証していない環境を対応と呼べない」という理由は本選択肢の却下には使えない**——採用案 (B) も Server 2019 を検証せずに対応と呼ぼうとしており、同じ矛盾を抱えるためである（この点は決定 1 のゲートで処理する）
- **(B) 対応 = Server 2019 以上 / 推奨 = Server 2022 以上の 2 段（採用）**: Server 2019 の残存ライフサイクルは 2029-01-09 までで、**v1.0 系列のサポート期限（決定 7 の 2028-11-14）を完全に覆う**。一方 Server 2016 の残存は約 6 か月であり、v1.0 の公開時期が未定である以上、重なる可能性はあるが**重なっても最大で数か月**である。その数か月のために単発検証・不具合対応・文書の説明責任を恒常的に負うことは、**ADR-0009 が x86 について適用した「需要の裏付けが無い対応範囲の恒常保守コストを負わない」という判断と同型**である。これは事実命題ではなくコスト比較として本 ADR の根拠に置く
- **(C) 対応 = Server 2022 以上に統一する**: 却下。検証コストは最も小さく TLS 1.3 も揃うが、**延長サポートが 2029 年まで残る Server 2019 環境を切ることになり、ADR-0001 が定めた対象利用者（中小企業の Windows 管理者）と衝突する**。ただし**この却下は、委任事項 1 の実機検証で Server 2019 に TLS 1.3 以外の重大な差が出た場合には撤回する**（再評価トリガ）
- **(D) 下限を設けず「動作報告を募る」形にする**: 却下。v1.0 は「本番利用を推奨できる」宣言であり、推奨の範囲を利用者に決めさせるのは宣言の放棄にあたる

**対応範囲の増減を判断する規則**（ADR-0009 との基準の一貫性を検証可能にするため明文化する）:

- **新規の拡大**（未表明の環境を対応に加える）には、**需要の一次情報**（利用者からの要望・Issue へのリアクション等）を要する。ADR-0009 が x86 に適用し、本 ADR が Server Core に適用する基準
- **既公表範囲の縮小**（ADR-0009 の「Windows Server 全般」から削る）には、**ライフサイクル・機能・検証コストのいずれかの根拠**を要する。需要の一次情報は要求しない——縮小の可否を「切られる側の声が上がるか」に委ねると、声を上げる手段を持たない利用者の環境が黙って残り続けるため
- **既公表範囲の維持**（Server 2019）には追加の根拠を要しない。既に表明済みの範囲を維持することは新規の約束ではない

この規則により、x86（不拡大・一次情報なし）・Server Core（縮小・検証コスト）・Server 2016 以前（縮小・ライフサイクル）・Server 2019（維持）の 4 件がすべて同じ物差しで説明できる。

### ブラウザ要件を明記するか

- **(a) 「最新版の Edge / Chrome / Firefox / Safari」を要件として明記する（採用）**
- **(b) 書かない（現状維持）**: 却下。**既定構成では管理 UI が loopback 固定**であり（ADR-0004・configuration.md §4）、初回セットアップ・本番昇格・保持期間変更はサーバ機上のブラウザから行う。Blazor は IE 非対応（外部検証 (4)）であり、かつ **Server 2019 / 2016 には Edge が同梱されない**（同 (5)）ため、**追加ブラウザを入れていない素の状態では管理 UI に到達できない**。Server 2022 以降は Edge が同梱されるためこの問題は起きない。書かなければ「インストールは成功したのに設定画面が開けない」に着地し、ADR-0001 の導入体験の原則に反する

### ハードウェア要件をどう決めるか

- **(a) 実測に基づく算出式 + 検証済み構成の実測値で表現し、CPU・メモリの下限確定は M-6 に委ねる（採用）**
- **(b) 一般的な目安（例「2 コア / 4 GB 以上」）を書く**: 却下。architecture.md §5.2 が定めた「公称値は基準環境での絶対値で判定する」原則と衝突する。**根拠のない数値を要件表に書くと、それが後から公称値の既成事実になる**（ADR-0006 運用の「量的な床も自己拘束の内側に置く」に反する）。**この却下理由は採用案にも適用する**——採用案が提示する算出式・目安値についても、確定値と暫定値を区別し、前提を明示することを決定 4 で義務づける

### 検証基盤をどう組むか

- **(a) 推奨環境は CI に加え、対応下限は単発検証する（採用）**: `windows-2022` ランナーは公開リポジトリで無料・無制限（[GitHub-hosted runners reference](https://docs.github.com/en/actions/reference/runners/github-hosted-runners)。ADR-0009 の「CI のアーキ別実行基盤」選択肢 (a) が確認した前提と同じ。確認日 2026-07-26）。一方 `windows-2019` ランナーは**退役済み**でランナー一覧に存在しない（同、確認日 2026-07-26）ため、Server 2019 は CI では踏めない。オーナー lab には Proxmox の x64 機があり、Windows Server 2019 の評価版（180 日・ISO/VHD で配布継続中。確認日 2026-07-26）を載せれば**追加費用ゼロで単発検証できる**
- **(b) Server 2019 を含む多 OS の恒常 lab を組む**: 却下（現時点）。常時保持は保守コストが個人運営規模に見合わない
- **(c) クラウド VM（Azure / AWS）で単発検証する**: 却下（第一選択としては）。ライセンス込みで評価版の期限・アクティベーション制約が無い利点はあるが、(a) が費用ゼロで同じ目的を達する。**Proxmox 機が使えない状況になった場合の代替として残す**

## 決定

**選択肢 (B) + (a) + (a) + (a)** を採用する。

### 決定 1: OS のサポート行列を「推奨 / 対応 / 試験的 / 対応外」の 4 区分で定義する

| 区分 | 対象 | 約束する水準 | 検証水準 |
|---|---|---|---|
| **推奨（Recommended）** | Windows Server 2022 / 2025（x64） | 全機能。ADR-0004 決定 3 の opt-in 強化 3 点（TLS 受信・閲覧 HTTPS・AD 連携認証）を含む。**性能公称値（M-6）を表明するのはこの区分に対してのみ** | Server 2025 = CI（`windows-latest`）+ 実機 lab E2E。**Server 2022 = CI のみ**（下記の非対称を参照） |
| **対応（Supported）** | Windows Server 2019（x64） | 中核機能（受信・保存・閲覧・管理・スプール・昇格）。**TLS を使う機能は 1.2 で成立する（実測。改訂 7）**。性能公称値は対象外 | 単発の実機検証（決定 5）。**実施済み = 2026-08-02。残条件 1 件（改訂 7 の A。改訂 8 で B は解消）**。CI 回帰の対象外 |
| **試験的（Experimental）** | Windows 11 on Arm 等のクライアント（ARM64） | ADR-0009 決定 2 の ARM64 行をそのまま継承（修正 SLA を約束しない） | リリースごとの `windows-11-arm` スモーク（ADR-0009 Phase 1） |
| **対応（Supported）** | Windows 11（現行版）・Windows 10 LTSC / Enterprise の .NET 10 対応版（x64） | 中核機能。試用・小規模用途を想定 | 開発機・lab メンバー機での実機使用 |
| **対応外** | Windows Server 2016 以前、Server Core / Nano Server、**Windows 10 Home / Pro**、32-bit | — | — |

**Server 2016 以前を対応外とする根拠**: **識別因子はライフサイクルの 1 点のみである**（残存約 6 か月。選択肢 (B) のコスト比較を参照）。TLS 1.3 非対応・Edge 非同梱は Server 2019 にも等しく当てはまるため 2016 特有の根拠にはならない——**この 2 点を根拠として数えない**。Server 2012 / 2012 R2 についてはこれに加えて VC++ 再頒布可能パッケージの前提（外部検証 (1)）がある。

**Windows 10 Home / Pro を対応外とする根拠**: ①**.NET 10 の対象外**である（外部検証 (1)。Windows 10 の対応は LTSC・Enterprise に限られる）。②**2025-10-14 に提供終了済み**（同 (6)）。Server 2016 に適用したライフサイクル基準を、クライアント側にも同じ物差しで適用した結果である。なお lab のメンバー機実績（「Windows 10 Pro」）はこの区分に当たるため、**対応表明の裏付けとしては使わない**。

**Server Core / Nano Server を対応外とする根拠**: **未検証だからである**。当初案は「管理 UI がブラウザを前提とする製品構造と原理的に噛み合わない」としていたが、これは誤りであり撤回する——**ADR-0004 決定 2 は Server Core を明示的に想定し、「既定構成の管理手段は設定ファイルの手編集となる。GUI での管理は認証 opt-in 後のリモート管理で行う」と定めている**。configuration.md §3（CF-5）はさらに `sc control Yagura 128` による再読み込みを「**Server Core・管理画面に入れない状況でも手編集を受信断なしで反映できる**」ことを理由に採用している。すなわち製品側に経路は設計されている。本 ADR が対応外とするのは、その経路を一度も実機で検証していないためであり、「動作しない」という主張ではない。**Server Core での需要が一次情報として確認された場合は再評価する**（再評価トリガ）。

**ADR-0009 決定 2 の x64 行の部分 supersession**: 本 ADR は ADR-0009 のアーキテクチャ軸の判断（x64 = 対応 / ARM64 = 試験的）を反転させないが、**同決定 2 の x64 行が無条件文として書いた対象範囲を条件文へ書き換える**。docs/adr/README.md の判定の目安（「疑わしい場合は最小の単位で反転の有無を検証する」）を最小単位「x64 × 特定 OS」に適用すると、Server 2016 / 2012 系・Windows 10 Home/Pro はいずれも「対応 → 対応外」の反転にあたるため、**同ルールの「疑わしければ supersession 側に倒す」に従い部分 supersession として扱う**。ADR-0009 の status は accepted のまま維持し、①一覧表の状態欄と②決定 2 の該当行に注記を加える（委任事項 9）。

**「対応」と「推奨」の差の意味**（ADR-0009 決定 2 の「サポート水準の定義」と同じ作法で明文化する）:

- **設計原則は全区分共通**: ADR-0001 の品質原則（ログを失わない・失った場合に必ず観測できる）は Server 2019 でも設計上の原則として適用される。区分によって「観測できない喪失を許容する」ことはしない
- **意図的な機能差は設けない（方針の言明）**: Yagura の実装都合で区分ごとに機能を削ることはしない。**ただし「実際に観測される差は TLS 1.3 のみである」とは言えない**（改訂 7 の実測）。差は少なくとも次の 2 点からなる:
  - ①**TLS 1.3 が使えない**（Schannel の対応が Server 2022 / Windows 11 以降のため）。TLS を使う 3 か所はいずれも TLS 1.2 へ縮退して成立する
  - ②**ハンドシェイクで実際に選ばれる暗号スイートは OS の Schannel 設定に依存する**。Yagura が固定するのはプロトコル版のみでスイート選択は OS に委ねる。**この点で Server 2019 と Server 2022 の既定は完全に同一である**（いずれも 31 種。改訂 8 の実測）——**弱いスイートの残存は対応区分固有の性質ではない**。区分間で実際に差が出るのは Server 2025 のみで、その差は **3DES と DHE-RSA 2 種が既定から外れている**という 1 点である

  したがって「対応環境は推奨環境より暗号強度が劣る」とは**言えない**。**残るのは全区分共通の注意**である: 既定では **SHA-1 の CBC スイート**が有効なままで、それしか提示しない相手とはその強度で成立する（3DES は 2019 / 2022 でのみ該当）。強度を統制したい場合は OS 側のハードニングで絞る。なお **NULL 系スイート（暗号化なし）は既定の一覧に載っているが、Microsoft は「アプリケーションが明示的に要求した場合にのみ使われる」と定めており、Yagura は要求しない**——一覧に載っていることを「平文で成立しうる」と読まないこと
- **性能特性の同等性は主張しない**: Server 2019 での持続流量・OS 受信バッファ挙動（M-2 の 1 MiB 既定を含む）の実測は無い。**性能公称値（M-6）は推奨区分に対してのみ表明する**
- **検証の裏付けが非対称**: 推奨環境は CI で継続検証する。対応環境は決定 5 の契機に該当したときの単発検証にとどまる。**この検証水準は、ラベルが下位である ARM64（リリースごとのスモーク + スプール 1 サイクル）より弱い**。この逆転を放置しないため、委任事項 1 に ARM64 と同水準の負荷項目を課した（⑤⑥）。**両項目は 2026-08-02 の lab 検証で通過している**（改訂 7）——スプール発動 → drain 追いつきは突合成立（5,000 = 5,000 + 0）、SQLite → SQL Server 昇格も件数損失ゼロで通し
- **不具合対応**: Server 2019 固有の不具合報告は受け付け、再現・修正を試みるが、修正の優先度は推奨環境の品質維持を優先して判断する
- **対応下限の二重劣後**: 対応下限（Server 2019 / SQL Server 2019）はいずれも Microsoft の**メインストリームサポートが終了済み**（2024-01-09 / 2025-02-28）であり、ベンダから提供されるのはセキュリティ更新のみである。Yagura 側でも修正優先度が劣後するため、**この区分は OS・DB ベンダとアプリベンダの双方で劣後する**。この合成をシステム要件表にも記す（委任事項 3）

### 決定 2: SQL Server は「2019 以上を対応・2022 以上を推奨」とし、未検証バージョンには表明ゲートを掛ける

| 区分 | 対象 | 根拠 | 表明ゲート |
|---|---|---|---|
| **推奨** | SQL Server 2022（Express・Standard・Enterprise） | 延長サポート 2033-01-11。Express は M7-2 で実測済み | Express のみ実測済み。他エディションは未検証（下記） |
| **推奨** | SQL Server 2025 | 延長サポート 2036-01-06 | **実測ゼロのまま（2026-08-08 の lab では未インストールで実施できず）。ゲートは閉じたまま——README では「検証中」を維持する** |
| **対応** | SQL Server 2019（同上） | 延長サポート 2030-01-08。v1.0 のサポート期間を覆う | ✅ **ゲート通過（2026-08-08。改訂 10）**。Express 15.0.2000.5 で 7 項目すべて合格し README へ転記済み |
| **対応外** | SQL Server 2017 以前 | 2017 は 2027-10-12 に終了予定で v1.0 のサポート期間に収まらない。**2016 は 2026-07-14 に終了済み** | — |

- **エディションは問わない**が、**Express の適用限界は database.md §5.3 のとおり利用者向けに明示する**（DB 最大 10 GB・バッファプール 1,410 MB・4 コア）。ゼロ設定ファーストランの既定は SQLite であり、Express は「無償で本番昇格したい場合の選択肢」として位置づける
- **エディション差は未検証**である。Standard / Enterprise は Express の制限を持たないため機能面の劣化は想定しないが、実測はしていない
- **LocalDB は運用形態としては対応外**とする。常駐サービスの保存先として設計された製品ではなく（ユーザーセッションに紐づく起動モデル）、Yagura が仮想サービスアカウント / gMSA で動く前提と噛み合わない。**ただし開発・CI の検証環境としては現に使用している**——「CI の現況」のとおり SQL Server 適合テストは LocalDB 上で全 PR 実走しており、**SQL Server provider の常設・blocking な自動検証はこの対応外の実行形態の上にのみ成立している**。ADR-0006 基準 1 の証拠として適合テスト記録を提出する際は、**この事実を明示する**（黙って提出しない）。実インスタンスに対する CI 実行の要否は委任事項 5 で判断する
- **組み込み DB（SQLite）は全区分で同一**とする。ネイティブ資産はアーキ別に self-contained publish へ含まれる（ADR-0009 決定 3・Phase 0 で検証済み）ため、**DB 製品の別途導入も OS 側の前提も持たない**。OS バージョンによる挙動差の有無は委任事項 1 の単発検証で確認する（差が無いことを前提に置かない）

### 決定 3: ブラウザ要件を明記し、管理 UI の到達性の前提として位置づける

- **要件**: 最新版の Microsoft Edge / Google Chrome / Mozilla Firefox / Apple Safari のいずれか（Blazor の対応ブラウザに従う。外部検証 (4)）。**Internet Explorer は対応しない**
- **要件が掛かる場所を明示する**: 閲覧 UI（8514）は**任意の端末のブラウザ**から開ける。日常のログ閲覧にサーバ機のブラウザは要らない。一方 **管理 UI（8515）は既定構成で loopback 固定のため、サーバ機自身に対応ブラウザが必要**である（管理リモート HTTPS を opt-in で有効にすれば〔ADR-0010〕リモート端末のブラウザでよい。その有効化は管理 UI からでも**設定ファイルの手編集からでも**行えるため〔ADR-0002 決定 5〕、サーバ機のブラウザは厳密には必須ではないが、**既定構成の導線ではサーバ機のブラウザを通る**）
- **推奨環境（Server 2022 / 2025）では Edge が同梱されるため追加作業は要らない**。対応環境（Server 2019）では**対応ブラウザの導入が導入手順の前提条件になる**。この前提は**部分的に実測で裏付けられた**（改訂 7 §C）——Windows Update を 2022-11 水準まで当てた Server 2019 に Edge は導入されておらず、既定ブラウザは Internet Explorer で、IE から管理 UI を開くと `document.body` が `null`（実質白画面）になる。**完全な裏付けには至っていない**（lab イメージに Chrome が既設で「素の状態」を作れなかった）。ただし Microsoft の公式文書は Windows Update による Edge の自動配信を **Windows 10 クライアントの範囲でしか記述しておらず**、Windows Server への自動配信を約束する記載は無い（[Windows updates to support Microsoft Edge](https://learn.microsoft.com/en-us/deployedge/microsoft-edge-sysupdate-windows-updates)、確認日 2026-08-02。Server での配備は Configuration Manager / Intune 等による明示的な配布として記述されている）ため、**前提が崩れる公算は小さいと判断し、要件の記述は維持する**
- **閉域環境での成立性**: README のセキュリティ前提はインターネットに露出しない設置を推奨しており、「最新版のブラウザを維持せよ」という要件と衝突しうる。**この解き方（オフラインインストーラの入手手順・更新の維持責任の所在・要件を満たさない場合の症状）は利用者向け文書で明示する**（委任事項 3）。要件だけを書いて成立手段を書かない状態にはしない

### 決定 4: ハードウェア要件は「provider 別の算出式 + 条件付きの実測値」で表現し、CPU・メモリの下限確定は M-6 に委ねる

- **ディスクの算出式は provider 別に分け、DB 側とボリューム側を分離する**:

  **DB 側（Express の 10 GB 上限と比較できる値）**
  - SQLite: `DB サイズ ≒ (平均メッセージ長 + 95 B) × 秒間流量 × 保持期間の秒数`
  - SQL Server: **未確定**。上記の単価はレコード単価の SQLite ファイルサイズ実測（M7-2）に基づくが、**SQL Server は全文字列列が `nvarchar`（UCS-2/UTF-16）で `_UTF8` 系照合順序を付与しない方針**のため（database.md §5.4）、ASCII 本文は 1 文字 2 バイトで格納される。**SQLite の単価は転用できない**。委任事項 2 の実機検証に容量計測を追加して確定するまで、SQL Server 版の式は公表しない

  **ボリューム側**
  - `必要な空き ≒ DB サイズ + スプール使用量上限【暫定・M-12】 + 空き容量の下限 1 GiB【暫定・M-16】`

  **確定値と暫定値を区別して表記する**（【暫定】の注記を要件表でも維持する）。選択肢 (b) を却下した理由（根拠のない数値の既成事実化）は自案にも適用する。**「安全率」は定義できないため式から外し、本式の見積もりが下限であること（索引・トランザクションログ・システムイベント・削除遅延分を含まない）を明記する**
- **既定構成の目安を載せる場合は前提を必ず併記する**: 保持 30 日・**平均メッセージ長 約 200 B**・10 msg/s で **約 7.8 GB**（DB-1 が Express 10 GB 上限への収まりを判定するために算出した値であり、**スプール・空き容量下限を含まない裸値**）。**この値は M-16 の Express 容量接近警告の閾値（10 GB × 0.8 = 8 GB）のすぐ下にある**——既定運用を Express で続けると定常状態で容量接近警告が出うることを、利用者向け文書で必ず伝える（委任事項 3）
- **CPU・メモリの下限は本 ADR では確定しない**。architecture.md §9 **M-6** が管轄する。本 ADR は M-6 に「**基準環境の定義は決定 1 の推奨区分に含まれる構成から選ぶ**」という制約を追加する（委任事項 8）。推奨区分は 2 バージョンあるため、**選ばれなかった側のバージョンについて公称値をどう扱うかも M-6 で決める**
- **参考実測値には条件を必ず並記する**: 「8 論理コア / 7 GiB・SQL Server 2022 Express 同居・単一マシン・1 回の実測」における Express ≈ 4,500〜5,000 msg/s・SQLite ≈ 15,000 msg/s（M7-2）。**これは性能上限を測るための構成であって推奨構成ではない**ことを、要件表では誤読されない文言で書く（委任事項 3）

### 決定 5: 検証水準は「推奨環境は CI・対応下限は単発」の 2 段とする

- **推奨環境**: `.github/workflows/` のジョブに **`windows-2022` を加える**。公開リポジトリの標準ランナーは無料・無制限のため追加費用は発生しない。**ただし CI で得られるものを過大に見積もらない**——回帰ベンチの blocking 判定は突合成立のみであり（M-5 v2）、`windows-2022` の追加で得られるのは「**Server 2022 でも突合が成立する**」ことである。**性能退行の検知は M-6 の管轄**であって CI の追加では得られない
- **委任先が判断できるよう、次の制約を先に置く**（委任事項 4）:
  - **OS バージョン差が現れるのは `installer-e2e` 相当のジョブ**（Windows サービス登録・ACL/監査・MSI の起動条件・証明書ストア）である。`ci.yml`（ビルド + 単体テスト + 短縮ベンチ）への追加では OS 差の被覆はほとんど増えない
  - `installer-e2e.yml` は `pull_request` の paths フィルタ付きであり、ここへの追加は決定 1 の表が言う「CI」の常時性を満たさない。**「常設」の語が指す実体を委任事項 4 で確定する**
  - `build-and-test` を matrix 化すると job 名が変わり、**ブランチ保護の必須ステータスチェック設定の更新（リポジトリ設定。外部 PR では完結しない）が同時に必要**になる
  - **基準値ファイルを OS 別に分けるか単一のまま使うかを判断する**。M-5 v2 が踏んだ分布混合による誤較正の再発を防ぐため。あわせて既存 baseline の出自（Server 2022）と現行実行環境（Server 2025）のずれの是正要否も判断する
  - `installer-e2e.yml` の「ci.yml には触れない（並行 PR との衝突回避）」という既存方針との関係を整理する
- **推奨区分内の検証の非対称を明示する**: AD 連携認証・gMSA・MSI のアップグレード挙動は **CI では原理的に検証できない**（conventions.md）。これらの実機裏付けは **Server 2025 の lab のみ**であり、**Server 2022 では AD 系が一度も踏まれていない**。Server 2022 での lab 実施要否は委任事項 10 で判断する
- **対応下限（Server 2019）**: **オーナー lab の Proxmox（x64）上に Windows Server 2019 評価版の VM を立てて単発検証する**。`windows-2019` ランナーは退役済みで CI では踏めない
- **単発検証を再実施する契機**: ①v1.0 リリース判定時、②対応下限の表明を変えるとき、③**Server 2019 の挙動に影響し得る変更**が入ったリリース。③は「インストーラに変更が入ったリリース」では毎回該当して運用不能になるため、**対象を具体的なパスで機械的に定める**（確定は委任事項 4。確定内容は改訂 2 を参照）。それ以外では再実施しない
- **受け入れるトレードオフ**: 上記③に該当しないセキュリティ修正（パーサ・認証・UI・監査経路など）は、**対応環境で一度も検証されずに出荷され得る**。その場合の対応環境での動作は「設計上共通であること」に依拠する

### 決定 6: MSI の起動条件で対応下限（OS ビルド番号）を機械的に強制する

- `installer/Package.wxs` に **OS ビルド番号による LaunchCondition を追加する**。**Windows Server 2019 と Windows 10 1809 はいずれもビルド 17763** であり、単一の条件式で表現できる
- **本条件の射程を正直に限定する**: この条件が新たに止めるのは実質「**Server 2016 以前**」だけである。**Server Core / Nano Server はビルド番号で判別できない**ため素通しする——決定 3 が特定した「入るのに管理 UI に到達できない」に最も確実に着地するのは Server Core であり、**本条件はその最悪ケースを捕まえない**。Server Core の判別手段の有無と、判別できる場合の拒否要否は委任事項 6 で調査する
- **`LaunchCondition` を採る理由**: `installer/Package.wxs` は利用者入力の検証について「`LaunchCondition` は UI シーケンスの先頭（利用者入力の前）にも評価されるため使えない」として type 19 エラー CA を採用済みである（同ファイル 217-219 行）。OS バージョンはシーケンス開始時点で確定し利用者入力に依存しないため、本件では `LaunchCondition` が適合する
- **アップグレード経路にも掛かる**: `LaunchConditions` は `InstallExecuteSequence` の先頭付近で評価されるため、**メジャーアップグレードも拒否される**。既定シーケンス上 `RemoveExistingProducts`（ADR-0023 決定 2 により `afterInstallExecute`）より手前で走るため、拒否されても ADR-0023 が扱った「製品が 1 つも残らない」には至らず**既存インストールは無傷で残る**。ただしその利用者は**新版を受け取れない**——決定 7・SECURITY.md の「最新リリースのみ・バックポートなし」と合成すると、**対応外 OS の既存利用者には脆弱性修正の受領経路が無くなる**。この帰結を利用者向け文書に明記する（委任事項 3・7）
- **誤判定のリスク**: Windows Installer は Windows 10 以降の互換性マニフェストを持たないため、`VersionNT` / `WindowsBuild` が実ビルド番号を返さない可能性がある。該当すると条件式は**全対応環境で false となり、新規インストールとアップグレードの両方が全環境で拒否される**。アップグレードでも評価されるため、これを含んだリリースは**全既存環境を更新不能にし、以後の修正の配布経路を断つ**。**検証は 4 区分 × 2 経路（新規 / アップグレード）で行い、推奨・対応区分で正常に通ることの確認を、対応外での拒否確認と同格の必須項目とする**（委任事項 6）
- **メッセージは「なぜ入れられないか」と「どうすればよいか」を含める**。対応する OS の下限・システム要件表への導線に加え、**対応外 OS の利用者が取れる選択肢**（移行の案内、既存 v0.x の扱い、相談先）を含める

### 決定 7: サポート表明の期限を .NET 10 のライフサイクルに接続する

- Yagura は .NET 10（LTS・サポート終了 **2028-11-14**）に self-contained で依存する。**v1.0 系列のサポート表明はこの日付を超えない**
- **.NET のメジャー更新は v1.0 系列の中では行わない**。**当初案はこれを「ADR-0006 基準 4（後方互換性の凍結）と同じ扱い」と書いたが、援用の誤りであり撤回する**——基準 4 の凍結対象は「設定スキーマ・DB スキーマ・保存データ形式」であり、ランタイムのメジャーバージョンを含まない。据え置きの根拠は独立に置く: **self-contained 配布ではランタイム更新が全成果物の再ビルド・再署名・再検証を伴い、v1.0 系列内での挙動同一性の担保コストが大きい**ため。この判断の代替案（v1.x で次期 LTS へ載せ替える / self-contained をやめる / セキュリティ修正のみ延長する）は本 ADR では比較していない——**比較していないことを明記する**
- **self-contained 配布の帰結（配布責任の移転）**: .NET ランタイムは MSI に同梱されるため、**ランタイムの脆弱性修正は Windows Update では利用者に届かない**。Yagura が再ビルド・再署名・再配布して初めて届く。**この責任範囲と対応方針（追随の有無・契機）を SECURITY.md に明記する**（委任事項 7）。方針が「自動監視は行わない」であっても、明記しないまま放置するよりよい
- **期限の逆転を利用者に見える場所へ書く**: v1.0 系列の期限（2028-11-14）は、**対応環境の OS・DB のいずれよりも先に来る**（Server 2019 = 2029-01-09、SQL Server 2019 = 2030-01-08、Server 2022 = 2031-10-14）。「OS が生きている限り Yagura も使える」と読まれるのが自然な導線であるため、**SECURITY.md だけでなくシステム要件表にも併記する**（委任事項 3・7）
- **既存ポリシーとの合成結果も明記する**: SECURITY.md の現行方針は「修正は最新リリースと `main` に提供・バックポートしない」である。決定 7 の期限と合成すると、**利用者が修正を受け取る条件は「最新版に追随し続けること」のみ**になる。この合成結果を SECURITY.md に書く（委任事項 7）

## 帰結

- **良くなること**:
  - 導入判断の最初の問い（「うちの環境で動くのか」）に文書で答えられるようになり、ADR-0006 基準 3 の穴が塞がる
  - Server 2022 が CI で踏まれるようになり、「推奨に挙げる環境がどのジョブでも踏まれていない」状態が解消される（ただし得られるのは突合成立の継続確認であり、性能回帰の検知ではない）
  - **Server 2016 以前への新規インストールをインストーラが止める**（Server Core は止まらない——決定 6 の射程参照）
  - サポート表明の期限と、その根拠・合成結果（バックポートなし・ランタイム同梱）が外部のライフサイクルに接続され、利用者が実効的な保証範囲を判断できるようになる
  - 起案の過程で **[Issue #482](https://github.com/Yanai-Taketo/Yagura/issues/482)（TLS 接続ハンドラの無音脱落）を発見**した。ADR-0006 基準 2 の重大不具合定義①に該当する欠陥であり、v1.0 の前に解消が必要
- **悪くなること（受け入れるトレードオフ）**:
  - **説明責任が増える**。「Server 2019 は対応だが検証は単発」「Server Core は未検証ゆえ対応外」「Windows 10 は LTSC/Enterprise のみ」といった非直感的な制約を一貫して書き続ける必要がある
  - CI の実行時間とジョブ数が増える。ブランチ保護の設定変更も伴う
  - **公表済みのサポート範囲を縮小する**。ADR-0009 の「Windows Server 全般」から Server 2016 以前・Server Core・Windows 10 Home/Pro を撤回する。現時点で該当環境の利用報告は無いが、**いないことの確認もできていない**
  - 決定 5 の契機③に該当しないセキュリティ修正は、対応環境で未検証のまま出荷され得る
- **リスク**:
  - **Server 2019 の単発検証で中核機能に不成立が出る可能性がある**。特に TLS 1.3 固定指定が Issue #482 の無音経路に落ちる場合、送信元が痕跡なく全滅しうる。**本 ADR が accepted でも、この検証を通過するまで Server 2019 の区分は「暫定」であり、README へ転記しない**
  - **決定 1 の Server 2019 行が外れた場合の波及が大きい**——選択肢 (C) へ倒れれば決定 3・5・6 と委任事項 1/3/6 が連鎖して書き直しになる
  - 決定 4 が CPU・メモリの下限と SQL Server のディスク算出式を確定しないため、**v1.0 のシステム要件表は「サイジングに使える数字が乏しい」状態で公開される**。大量ログ環境の導入検討者は判断材料を得られない。**ADR-0006 基準 1 は性能公称値の充足を求めており、M-6 は v1.0 のクリティカルパスである**——本 ADR はこれを「先送りできる宿題」として扱わない
  - **SQL Server 2019・2025・Express 以外のエディションは実測ゼロ**であり、ライフサイクル上の妥当性だけを根拠に区分している。ドライバ側の下限が公式に確認できなかったこと（検証の限界）と合わせ、この表明は推論に立つ
  - 決定 6 の条件式が誤判定した場合、**全環境で更新不能になる**（決定 6 の誤判定リスク）

## 先送りにする場合の再評価トリガ

- **Server 2019 の単発検証で TLS 1.3 以外の重大な差、または中核機能の不成立が出た場合**: 選択肢 (C)（対応 = Server 2022 以上）への転換を supersession で検討する
- **委任事項 1 の負荷項目（⑤⑥）が通らない場合**: Server 2019 の区分を「試験的」へ落とすことを検討する（ARM64 との検証水準の整合）
- **SQL Server 2019 の性能が 2022 Express と同オーダーに収まらない場合**: 決定 2 の対応区分を見直す
- **Windows Server 2022 のメインストリームサポートが終了した場合（2026-10-13。約 3 か月後）**: 推奨区分の表現を点検する（延長サポートは 2031-10-14 まで残るため区分自体の変更は要さない見込み）
- **Windows Server 2019 の延長サポート終了（2029-01-09）が近づいた場合**: 対応区分から外す。v1.0 系列のサポート期限（2028-11-14）より後であり、実質的には次のメジャーバージョンの論点になる
- **Server Core での需要が一次情報として確認された場合**: ADR-0004 決定 2 が定めた手編集 + リモート管理の経路を実機検証し、対応区分への追加を検討する
- **GitHub が `windows-2022` ランナーを退役させた場合**: Server 2022 を単発検証側へ移す

## 委任事項の一覧（追跡用）

追跡 Issue は委任事項 11 で採番する。

| # | 委任事項 | 追跡 | 委任先 | 内容 |
|---|---|---|---|---|
| 1 | **Server 2019 の単発実機検証**（区分確定の前提ゲート） | 🟡 **条件付き実施済み（2026-08-02。改訂 7）**。残条件は [#494](https://github.com/Yanai-Taketo/Yagura/issues/494) に残す（手順書: [`installer/lab/server2019-support-validation-procedure.md`](../../installer/lab/server2019-support-validation-procedure.md)） | lab 手順書 + 実装 PR | Proxmox 上の評価版 VM で実施。**必須項目**: ①`Tls12｜Tls13` 固定指定の挙動を 3 か所すべてで確認——(i) TLS 1.2 へ縮退か / 例外か / 起動時か接続時か、(ii) 例外型が既存 catch フィルタに一致するか、(iii) **失敗が観測可能か**（Issue #482。観測できないなら実装是正が先）、(iv) **ネゴシエートされた暗号スイート**を推奨環境と比較。②MSI 導入 → 受信 → 保存 → 閲覧 → 管理 UI 到達。③**対応ブラウザ導入前の素の状態**で管理 UI に到達できないことの確認（「素の状態」の定義——ISO 直後か Windows Update 適用後か——を手順書で固定する）。④観測性カウンタの出力。⑤**スプール発動 → drain 追いつきの 1 サイクル**（ARM64 に課したのと同水準）。⑥**SQLite → SQL Server 昇格の 1 通し**（委任事項 2 と合同可）。⑦M-2 の受信バッファ既定（1 MiB）が有効か。実施記録を PR body に残す |
| 2 | **SQL Server 2019 / 2025 の実機検証** | [#495](https://github.com/Yanai-Taketo/Yagura/issues/495) | lab 手順書 + 実装 PR | 決定 2 が推論で置いた区分の裏付け。接続・スキーマ作成・照合順序検査（`sys.fn_helpcollations()`）・昇格ウィザード・保持期間削除に加え、**①`ProviderWriteCeiling` を 1 回走らせ M7-2 の 2022 Express 実測と同オーダーであることの確認、②一定件数投入後の DB サイズ計測**（決定 4 の SQL Server 版算出式の確定に必要） |
| 3 | **README のシステム要件表の新設**（ADR-0009 委任事項 7 の土台） | ✅ #489 #492 | README + operations.md + Releases/リリースノート | 決定 1〜4・7 を利用者向けの言葉で。**含めるもの**: ブラウザ要件と管理 UI 到達性の関係（**閲覧は任意端末でよいことを併記**）・**閉域環境でのブラウザ導入と更新維持**・OS ビルド番号の確認方法（`winver`）・アーキ表との判定順序（または統合）・「まず試すのに要るもの / 昇格時に要るもの / ハードウェアの目安」の順序・**既定運用が Express 容量警告の閾値近傍にあること**・参考実測値が推奨構成ではないこと・**二重劣後**・**サポート期限の逆転**・対応外 OS の利用者への案内。**順序**: 広すぎる既存表明の是正（「Windows Server 全般」の削除・Server 2016 以前を対応外へ）は**委任事項 1 のゲートを待たず先行**させ、**Server 2019 / SQL Server 2019・2025 の新規表明のみ**をゲート後とする。ゲート通過前の表記（「検証中」等の中間表記の要否）も決める |
| 4 | **CI への `windows-2022` 追加** | ✅ #491 | `.github/workflows/` + 実装 PR | 決定 5 の制約 5 点（対象ジョブと「常設」の定義・ブランチ保護の必須チェック名・baseline の OS 別分割と既存 baseline の出自ずれ・installer-e2e の既存方針・再実施契機③のパス定義）を判断し、根拠を記録する。実行時間への影響も実測記録 |
| 5 | **SQL Server 実インスタンスでの CI 実走の要否判断** | ✅ **判断済み（改訂 11）: 採らない**。継続 = LocalDB / 単発 = 実インスタンス lab の 2 層を証拠提出時に明示する | 実装 PR | 適合テストは LocalDB 上で実走済み（決定 2）。判断対象は**対応区分内の実インスタンス（Express）での追加実走**の要否。採らない場合は「常設検証が対応外の実行形態の上にある」ことを ADR-0006 基準 1 の証拠提出時に明示する |
| 6 | **MSI の LaunchCondition 実装** | ✅ **完了（改訂 9 で実装 / 改訂 12 で拒否側を実測）** #496 | `installer/Package.wxs` + 実装 PR | ビルド 17763 を下限とする条件式。使用する MSI プロパティと比較方法の**実ビルドでの確証**（誤判定リスクの排除）、拒否メッセージの文言、**Server Core の判別手段の有無の調査**、**4 区分 × 2 経路（新規 / アップグレード）の検証**（推奨・対応区分で通ることを必須項目に含める） |
| 7 | **SECURITY.md の改訂** | ✅ #493 | SECURITY.md（ADR-0006 基準 5） | ①決定 7 の上限（2028-11-14）と根拠、②**同梱 .NET ランタイムの脆弱性対応の責任範囲と方針**、③**対応外 OS の既存利用者への修正提供方針**（不提供ならその旨）、④**既存の「最新リリースのみ・バックポートなし」との合成結果**の 4 点 |
| 8 | **M-6 への制約の反映** | ✅ #490 | architecture.md §9 M-6 行 + 同 §5.2 + conventions.md「CI 回帰ベンチの基準値更新」節 | 決定 4 の制約（基準環境は推奨区分から選ぶ / 選ばれなかった側のバージョンの扱い）を反映。homework.md の 3 点セット原則に従い**本 ADR を accepted にする PR と同一 PR で実施する** |
| 9 | **ADR-0009・ADR-0004 への注記** | ✅ #490 | ADR-0009 + ADR-0004 + docs/adr/README.md | ①ADR-0009 決定 2 の x64 行と一覧表の状態欄に「x64 行の対象範囲は ADR-0024 により部分的に superseded」を注記（status は accepted のまま）。②**ADR-0004 決定 2 の Server Core 記述と帰結の該当行**に、本 ADR で Server Core が対応外区分になった旨を注記。**accepted 化 PR と同一 PR で実施する** |
| 10 | **Server 2022 での lab 実施要否の判断** | ✅ **判断済み（改訂 11）: 実施しない**。非対称を本 ADR に記録し再評価トリガに載せる（要件表には書かない） | lab 手順書 + 実装 PR | 推奨区分でありながら AD 連携認証・gMSA・MSI アップグレードが Server 2025 でしか踏まれていない非対称（決定 5）への対処。実施するなら Proxmox 上の評価版で、しないなら要件表で非対称を明示する |
| 11 | **追跡 Issue の採番** | ✅ 本表 | Issue | 本 ADR の関連行と委任事項の表に Issue 番号を与える。委任事項 3・4・6・7 は外部貢献者が拾える粒度のため、番号があると着手宣言と重複回避ができる |

## 改訂履歴

### 改訂 1（2026-08-01）: 委任事項 3（README 分）・8・9 の実施結果を記録。決定は不変

- **委任事項 9（ADR-0009・ADR-0004 への注記）= 完了**。ADR-0009 の状態行と決定 2 の x64 行、ADR-0004 決定 2 の Server Core 箇条と帰結の該当行、および ADR 一覧表の 3 点セットへ注記を入れた。**両 ADR の status は accepted のまま維持**——本 ADR が限定したのは対象範囲であって、アーキテクチャの判断（ADR-0009）も手編集による管理経路の設計（ADR-0004）も反転していない
- **委任事項 8（M-6 への制約の反映）= 完了**。architecture.md §9 の M-6 行・同 §5.2・conventions.md「CI 回帰ベンチの基準値更新」節の 3 点へ、決定 4 の制約（基準環境は推奨区分から選ぶ / 選ばれなかった側のバージョンの扱いも M-6 で決める / 公称値の表明対象は推奨区分に限る）を反映した
- **委任事項 3（README のシステム要件表）= 部分完了**。README へシステム要件表を新設し、「Windows Server 全般」という広すぎる既存表明を是正した（Server 2016 以前・Windows 10 Home/Pro・Server Core を対応外へ）。**operations.md と Releases / リリースノートへの反映は未着手**であり、委任事項 3 は閉じていない

### 改訂 2（2026-08-01）: 委任事項 4 の実施結果を記録。決定は不変

決定 5 が委任先へ送った制約 5 点の判断結果と、CI への Server 2022 追加の実装。**決定 5 の委任は「対象ジョブの範囲を実装 PR で確定する」としか書いておらず、必須ステータスチェックの統治という論点を予見していなかった**——本改訂はその空白も埋める。

#### 判断 1: ランナーラベルは明示ピンにし、将来 OS への追従は別の目的として分離する

`windows-latest` を推奨環境の被覆に使うと、GitHub がラベルの指す OS を動かした時点で**推奨区分として何を踏んでいるかが暗黙にずれる**。これは既に一度起きている——`windows-latest` が Server 2022 だった頃の基準値が残ったまま実体が 2025 へ移り、「推奨に挙げた Server 2022 がどのジョブでも踏まれていない」状態が生まれた（文脈節）。

明示ピン（`windows-2025` / `windows-2022`）なら、推奨区分を変えるときに CI も必ず直すことになり**ずれが沈黙しない**。一方「最新に追従したい」という要求は別に存在するため、**`build-and-test-canary`（`windows-latest`）をカナリアとして分離**する。ゲートには含めない——対応を表明していない OS の失敗でリリースを止めないため。`continue-on-error` は使わない（緑に見えると兆候が埋もれる。**赤いまま非 blocking** にする）。現在は実体が重複するが、**本ジョブの価値は GitHub がラベルを移した日から生じる**。

#### 判断 2: ゲートの定義は workflow ファイルが持つ（集約ジョブ）

必須ステータスチェックは**ジョブ名**で指定され、その設定は**リポジトリ設定（オーナー専有）**にある。実作業ジョブを直接指定すると、OS の増減やジョブ名の変更のたびに設定更新が要り、**忘れると黙ってゲートが外れる**。

そこで固定名の集約ジョブ `build-and-test` を受け口とし、**ゲートの構成（何の成功を要求するか）を `needs` として workflow ファイル側に置く**。これにより変更は PR だけで完結し、外部貢献者も推奨区分の増減に追随できる。実作業ジョブ名には OS を明示できる（`build-and-test-recommended` の matrix）。

**この構成により Server 2022 の失敗はマージを止める**。決定 1 が推奨区分へ「全機能」を約束している以上、片方だけ非 blocking にする理由がない。

#### 判断 3: 実作業は matrix + composite action にまとめ、OS ごとの複製を作らない

判断 2 の集約ジョブを置いた時点で、**matrix を避ける理由（job 名に matrix 値が付いて必須チェック名が動く）は消える**。OS ごとに独立ジョブを並べるとジョブ本体が丸ごと重複するため、共通手順（.NET セットアップ・LocalDB 確保・restore・build・test）を **composite action `.github/actions/windows-build-test`** へ切り出し、matrix とカナリアが共用する。

#### 保守手順: 新しい OS を推奨区分へ加える／外す

**触るのは 3 か所だけで、リポジトリ設定の変更は要らない。**

1. **ADR-0024 決定 1 の表**を更新する（推奨区分の定義が正）
2. `.github/workflows/ci.yml` の matrix へ 1 行（`- os: windows-XXXX` / `bench: false`）
3. `.github/workflows/installer-e2e.yml` の matrix へ 1 行

外すときは同じ 3 か所から削る。ゲートは `needs: build-and-test-recommended`（matrix 全体）で表現されるため増減に自動追随する。**例外は回帰ベンチの担当 OS を移す場合**で、そのときは `bench: true` の付け替えに加えて conventions.md「CI 回帰ベンチの基準値更新」の手続きによる再較正が要る（基準値は 1 本で OS 別の区分を持たないため）。

#### その他の制約の判断

- **対象ジョブと「常設」の定義**: `ci.yml`（全 PR + main への push＝常設）と `installer-e2e.yml`（paths フィルタ付き＝常設ではない）の**両方**に足す。OS 差が最も現れるのは installer-e2e の検証面（Windows サービス登録・ACL・MSI の起動条件）であり、ci.yml 側は「推奨環境でビルド・単体テストが通る」の常設確認にとどまる
- **baseline の扱い**: 回帰ベンチは推奨区分のうち **1 つの OS でのみ**走らせる（matrix の `bench` フラグ）。基準値ファイルは 1 本で OS 別の区分を持たず、複数 OS の分布を混ぜると **M-5 v2 が実測で診断した分布混合による誤較正を再現する**。ベンチの blocking 判定は突合成立のみで、これは M-5 v2 の全 7 標本で成立した OS 非依存の機能保証である。**既存 baseline の出自ずれ**（Server 2022 で採取・現行の実行環境は Server 2025）は本 ADR の管轄外の別件として残す——比判定が既に非 blocking のため実害は情報記録の解釈に限られ、是正するなら基準値更新手続きに載せる必要がある
- **`installer-e2e.yml` の「ci.yml には触れない」既存方針との整合**: 同方針は**インストーラ側の作業が ci.yml を巻き込まない**趣旨（並行 PR の衝突回避）であり、OS 対応範囲という横断的な軸を両ファイルへ独立に足す本件は抵触しない（両変更は互いに依存せず片方でも成立する）
- **再実施契機③のパス定義（確定）**: 次のいずれかに変更が入ったリリースで、Server 2019 の単発検証（委任事項 1）を再実施する。それ以外では再実施しない。
  - `src/Yagura.Ingestion/Tls/**`（TLS 受信リスナ一式）
  - `src/Yagura.Ingestion/Tcp/TcpConnectionAcceptLoop.cs`（両リスナ共通の Accept ループ）
  - `src/Yagura.Host/Program.cs` の Kestrel HTTPS 構成（`UseHttps` / `SslProtocols` / `ServerCertificateSelector`）
  - `installer/Package.wxs` の起動条件（決定 6 の LaunchCondition）
  - `src/Yagura.Host/Administration/Https/**`（証明書ストア参照）

  選定基準は「**Server 2019 と推奨環境で OS 側の実装が異なる面に触れるか**」——具体的には Schannel（TLS プロトコル・暗号スイート）、証明書ストア、Windows Installer の版判定である。ビルド設定・DB provider・UI・パーサの変更はこの基準に当たらない（OS 差を持たない）。

### 改訂 3（2026-08-01）: 委任事項 3 を完了。決定は不変

改訂 1 で「部分完了」としていた委任事項 3 の残り（operations.md・Releases / リリースノート）を実施し、**委任事項 3 を閉じる**。

- **リリースノートのテンプレート**（`.github/workflows/release.yml`）: 「アーキテクチャ対応（ADR-0009）」節が **本 ADR で是正したはずの古い表明（「Windows Server 全般 + Windows 10/11 クライアント」）を保持したまま**だった——README を #489 で直した一方、**Releases ページの記載は古いままという不整合**が生じていた。ペルソナレビュー（佐藤）が「README のシステム要件表より先に Releases ページで MSI を落とす」と指摘した経路そのものであり、45 MB をダウンロードして実行してから拒否される体験を招く。決定 1 のサポート行列・ブラウザ要件・サポート期限を載せた「対応環境」節へ差し替えた
- **operations.md §12「対応環境を維持する」**: README（導入判断時に読む）と役割を分け、**運用を続けるうちに効いてくる側**を扱う——①閉域環境でのブラウザ更新の維持（要件と推奨設置形態の両立手段。管理リモート HTTPS を使えば必要性が下がることを含む）、②対応下限の二重劣後、③Yagura のサポートが対応 OS より先に切れること、④対応外 OS では新版を受け取れなくなること（決定 6 の LaunchCondition がアップグレードにも適用される帰結）

### 改訂 4（2026-08-01）: 委任事項 7 を完了。決定は不変

SECURITY.md の「サポート対象バージョン」節を「**サポート対象バージョンと、修正の届き方**」へ拡張し、決定 7 が求めた 4 点を記載した。

- **① サポート期限と根拠**: v1.0 系列は 2028-11-14（同梱 .NET 10 の LTS 終了日）。**対応 OS・DB のいずれよりも先に来る**ことを対照表つきで明示した
- **② 同梱 .NET ランタイムの責任範囲と方針**: self-contained 配布のため**ランタイムの修正は Windows Update では届かず**、Yagura の再ビルド・再署名・再配布を経由する。あわせて**同梱ランタイムの版が明示の依存として宣言されておらず**（`global.json` の SDK 指定が解決した版が焼き込まれる）、**Dependabot の監視対象に含まれない**ことを実ファイル（`.github/dependabot.yml` は NuGet と GitHub Actions のみを対象）で確認して記載した。対応方針はフォワーダ（Fluent Bit）の版運用と同型——**自動監視は行わず**、契機は「.NET の重大な脆弱性の公表時」と「各リリースの準備時」の 2 つ、所要期間は約束しない
- **③ 対応外 OS の既存利用者への修正提供方針**: 決定 6 の LaunchCondition が**アップグレードにも適用される**ため、対応外 OS の既存インストールは動き続けるが新版を受け取れない。バックポート不実施と合成すると**修正を受け取る経路が無くなる**ことを明記し、移行が難しい場合の相談先を示した
- **④ 既存ポリシーとの合成結果**: 「最新リリースのみ・バックポートなし」を裏返すと、**利用者が修正を受け取る条件は最新リリースへ追随し続けることだけ**である旨を明示した（版を固定して長く使う運用を予定する利用者への警告として）

これにより ADR-0006 基準 5 の「SECURITY.md が実態と一致している」の一部が前進する。**残るのは v1.0 リリース時点でのサポート対象バージョン表の更新**（現在は v0.5 を最新として記載）であり、これはリリース準備時の作業として残す。

### 改訂 5（2026-08-01）: 委任事項 11 を完了。決定は不変

未了の委任事項へ追跡 Issue を採番し、委任事項の表に**追跡列**を追加した。ペルソナレビュー（リサ）の「**委任事項は外部から手を挙げられる作業リストなのに、採番されたものが 1 つもない**——着手宣言する場所も重複を避ける手段もない」という指摘への対応である。

| 委任事項 | 追跡 |
|---|---|
| 1（Server 2019 の実機検証） | [#494](https://github.com/Yanai-Taketo/Yagura/issues/494) |
| 2（SQL Server 2019 / 2025 の実機検証） | [#495](https://github.com/Yanai-Taketo/Yagura/issues/495) |
| 6（MSI の LaunchCondition） | [#496](https://github.com/Yanai-Taketo/Yagura/issues/496) |
| 5・10（CI 実走の要否 / Server 2022 lab の要否） | [#497](https://github.com/Yanai-Taketo/Yagura/issues/497) |

**完了済みの委任事項（3・4・7・8・9）には Issue を立てず、実施 PR の番号を追跡列に記録した**——閉じた作業に新規 Issue を作ってもノイズにしかならないため。委任 5 と 10 を 1 つの Issue にまとめたのは、どちらも「実施するかどうかを決める」判断であり性質が同じであるため。

この時点で **11 件中 6 件（3・4・7・8・9・11）が完了**し、残る 5 件のうち **2 件（1・2）はオーナー lab の実機作業**、**1 件（6）は実装 + 実機検証**、**2 件（5・10）は判断**である。

### 改訂 6（2026-08-01）: 委任事項 1 の lab 手順書を作成。決定は不変

[`installer/lab/server2019-support-validation-procedure.md`](../../installer/lab/server2019-support-validation-procedure.md) を作成した（実施は未了）。委任事項 1 の必須項目 7 点を §A〜G に展開し、**それぞれに判定表と失敗時の分岐**を付けた。

- **「素の状態」の定義を確定した（オーナー裁定 2026-08-01）**: §C の「対応ブラウザ導入前の素の状態」は **(b) Windows Update を適用した後**とする。決定 3 が守ろうとしているのは「利用者が実際に遭遇する状態」であり、ISO 直後で確認しても現実の環境の答えにならないため。**この定義は結論が反転しうる分岐を含む**——Windows Update が Edge を導入した場合、「Server 2019 でも素で管理 UI に到達できる」ことになり、**決定 3 と README の記述を緩める方向で見直す対象になる**（失敗ではなく発見として扱う）
- **暗号スイートの比較（§A-4）を手段つきで具体化した**: `SslStream` の `NegotiatedCipherSuite` を推奨環境と並べて記録する PowerShell を手順書に置いた。決定 1 が「意図的な機能差は設けない」と方針を述べるにとどめている以上、**実際に観測される差の全体は本検証でしか確定できない**
- **失敗の観測可能性を独立の判定軸にした（§A-1）**: ハンドシェイクが失敗すること自体より、**失敗がカウンタ・ログに現れないこと**の方が重い（ADR-0001 の品質原則違反）。カウンタが動かない場合は「実装の是正が先、対応表明はその後」と判定表に明記した
- **§E（スプール発動 → drain 1 サイクル）を必須にした**: ADR-0009 が「試験的」の ARM64 に課したのと同水準を「対応」の Server 2019 にも課す。静的スモークではバックプレッシャ経路を踏めない

**実施はオーナー lab（Proxmox + Server 2019 評価版）を要するため、本 ADR ではここまでを完了とする。**

### 改訂 7（2026-08-02）: 委任事項 1 を条件付きで完了。決定 1 の対応行を確定、決定 1 の「差の内訳」を実測で書き換え

オーナー lab（Proxmox 上の Windows Server 2019 Standard Evaluation **10.0.17763.3650**、Desktop Experience）で改訂 6 の手順書を実施した。**実施できた範囲はすべて合格側**であり、**決定 1 の Server 2019 行の「暫定」を外す**。

#### 実施結果

| 項目 | 結果 |
|---|---|
| §A-1 syslog TLS 受信（6514） | **合格**。`SslProtocol = Tls12` / `CipherAlgorithm = Aes256` / `HashAlgorithm = Sha384` / `KeyExchange = ECDHE(384)` で Handshake OK |
| §A-2 管理 HTTPS（8516） | **合格**（6514 と同一値） |
| §A-3 閲覧 HTTPS（8514） | 実施不能（検証対象の版に機能が無い。下記「残条件」）。**ただしコード経路の同一性で実質的に被覆されている**——`Program.cs` の `ConfigureHttpsIfRequired` は管理面・閲覧面で共通の 1 か所であり、`SslProtocols` 指定も共有している。§A-2 の成立は同じ Kestrel 経路の成立を意味する |
| §A-4 暗号スイート比較 | 未実施（lab から推奨環境へ到達不能）。**2019 側の既定有効 31 スイートは全文採取した**（下記） |
| §B 中核機能の通し | **合格**（導入 → 受信 → 保存 → 閲覧 5/5 → 管理 UI 到達） |
| §C 素の状態のブラウザ不在 | 観測不能（lab イメージに Chrome 既設）。**部分観測は決定 3 を支持**（決定 3 に反映済み） |
| §D 観測性カウンタ | 成立（12 種を表示・初期値 0） |
| §E スプール発動 → drain | **合格**。退避 5,000（発動を確認）→ drain 完了 → **突合成立（5,000 = 5,000 + 0）**、損失系カウンタ全 0 |
| §F SQLite → SQL Server 昇格 | **合格**。切替後 200/200・切替前 3,000/3,000（損失 0）。受信断は製品自身の計測で約 1.7 秒 |
| §G 受信バッファ既定 | **合格**。「UDP 受信バッファサイズを 1048576 バイトに設定しました」= 1 MiB が丸めなしで適用 |

#### 決定への反映

1. **決定 1 の対応行を確定**（「暫定・検証待ち」を削除）。TLS は 1.2 で成立することが実測で確定した
2. **決定 1 の「意図的な機能差は設けない」bullet を書き換えた**。「差は TLS 1.3 の 1 点」という見込みは**維持できない**——Server 2019 の既定有効スイートに **3DES・SHA-1 系 CBC・NULL（暗号化なし）**が残っており、Yagura はスイート選択を OS に委ねるため、**弱いスイートを提示するクライアントとはその強度で成立しうる**。**これは Server 2019 固有ではなく全区分に掛かる性質**であり、推奨環境との相対差の確定は残条件（§A-4）へ送る
3. **決定 1 の「暫定」条項を解除**。ARM64 との検証水準の逆転を埋めるために課した ⑤⑥（スプール 1 サイクル・昇格 1 通し）は両方とも通過した
4. **決定 3 に §C の部分観測を反映**。要件の記述は維持する（判断根拠は決定 3 を参照）

#### 残条件（v1.0 リリース前に消す。#494 に残す）

**検証対象は v0.5.0（`7f638da`、2026-07-24）であった。**現在の main には、v0.5.0 に存在しない機能が 2 つある。

| 残条件 | 内容 | 消し方 |
|---|---|---|
| **A. 閲覧 HTTPS と `faulted` カウンタの実測** | 閲覧 HTTPS（ADR-0022。`97f655a`/`eee9933`、2026-07-26）と `yagura.ingestion.tcp_connection.faulted`（Issue #482 の修正。`e2e6562`、2026-08-01）はいずれも v0.5.0 より後。**§A-3 と §A-1 の観測性判定は、この 2 つを含むビルドで再測する** | main からビルドした MSI で §A-1・§A-3 のみ再実施 |
| ~~**B. §A-4 の推奨環境との比較**~~ | ~~2022 / 2025 側の既定スイート集合が未採取~~ → **改訂 8（2026-08-02）で完了**。2019 と 2022 の既定は完全に同一で、弱いスイートの残存は対応区分固有ではないことが確定した | CI（`tls-cipher-suite-survey.yml`）で採取済み |

**なお §A-1 の観測性は「不合格」ではない**。手順書が指定した観測手段（`faulted` カウンタ）は当時のビルドに無かったが、**失敗ハンドシェイク 5 回すべてに対しイベントログ警告が 1 件ずつ記録された**（`Yagura.Ingestion.Tls.TlsSyslogListener`、例外型とスタックを含む）。「失敗が観測できない」という不合格分岐には該当しない。

#### 検証で判明した製品側の欠陥（すべて現 main での残存をコードで確認済み）

| # | 内容 | 追跡 |
|---|---|---|
| 1 | 保存先が到達不能な間、状態画面と閲覧 UI が例外で開けない。**計器が最も必要な瞬間に観測手段が失われる** | [#500](https://github.com/Yanai-Taketo/Yagura/issues/500) |
| 2 | SQL Server へ昇格した直後の初回起動で、受信断区間の記録が必ず失敗する（スキーマ作成前に書き込むため） | [#501](https://github.com/Yanai-Taketo/Yagura/issues/501) |
| 3 | 昇格ウィザードの「退避 / 削除」が実行されない（既知未実装だが追跡 Issue が存在しなかった） | [#502](https://github.com/Yanai-Taketo/Yagura/issues/502) |
| 4 | 証明書秘密鍵の ACL 付与が権限不足で失敗したとき、是正手順が案内されない | [#503](https://github.com/Yanai-Taketo/Yagura/issues/503) |

**`faulted` カウンタが状態画面の用語対応表に未登録**（Issue #482 の修正の積み残し。画面に「（対応表未登録の項目）」と出る）である点も本検証の過程で判明したため、本 PR で修正した（`ui.md` §7.2 と `UiText.SystemStatus` へ登録）。

#### 手順書への反映（改訂 6 の誤りの是正）

実施を通じて手順書側の誤りが 3 点判明したため、同時に是正した。詳細は手順書の冒頭注記を参照。

- **Probe スクリプトが Server 2019 上では必ず失敗する**——PowerShell 5.1 の `SslStream` は既定提示が TLS 1.0 までで、Yagura の下限 1.2 と共通アルゴリズムを持たない。`[SslProtocols]::Tls12` の明示が要る。**これは製品の運用情報でもある**（Server 2019 上の古い送信側は既定設定のままでは接続できない）ため operations.md にも記した
- **§F が「切替中の受信継続」を確認せよと書いていた**——昇格は再起動方式であり、製品自身が「反映にはサービスの再起動が必要です（再起動中は受信できません）」と表示する。**設計上存在しない挙動を確認項目にしていた**
- **§E の「SQLite なら書き込み不能にする」が非破壊では成立しない**——稼働中プロセスの開き済みハンドルに ACL 変更が効かない。SQL Server 停止方式を正式手順とする

**加えて「MSI は main からビルドしたものを使う」を明記した。**今回の残条件はすべてこの指定漏れに起因する。

### 改訂 8（2026-08-02）: 委任事項 1 の残条件 B（暗号スイート比較）を完了。決定 1 の差の内訳を実測で確定

改訂 7 が残条件として送った §A-4（推奨環境との暗号スイート比較）を、CI（`.github/workflows/tls-cipher-suite-survey.yml`）で採取して完了した。**lab は使っていない**——測っているのは Yagura の挙動ではなく OS の既定設定だからである。

#### 測定でつまずいた点（方法の記録）

**`Get-TlsCipherSuite` の出力は OS 既定ではない場合がある。** GitHub ホストランナーは 2022 / 2025 とも**グループポリシーでスイート順序を上書き**しており（強い 10 種のみに絞る image 側のハードニング）、同コマンドはそのポリシーの内容を返す。初回計測は「10 種・弱いスイート 0」となり、Microsoft が公表する Server 2022 の既定（31 種）と食い違った。**この食い違いに気づかず記録していれば、「推奨環境に弱いスイートは無い = 2019 固有の差」という逆の結論を ADR に書くところだった。**

OS 既定は `HKLM\SYSTEM\CurrentControlSet\Control\Cryptography\Configuration\Local\SSL\00010002` が持つ。以降これを主測定とし、GPO キーと `Get-TlsCipherSuite` の実効値は「どちらを見ているか取り違えないため」に併記する。

同コマンドは実行系でも返り方が割れた（pwsh 7 = 全スイートが 1 オブジェクトへ潰れる / Windows PowerShell 5.1 = 何も返さない）。いずれも例外を出さないため、件数ガードを入れて沈黙を落とすようにした。

#### 測定結果（OS 既定の集合）

| | Server 2019 | Server 2022 | Server 2025 |
|---|---|---|---|
| 出典 | lab 実測（10.0.17763.3650、2026-08-02） | CI 実測（`windows-2022`） | CI 実測（`windows-2025`、10.0.26100.32995） |
| 既定有効スイート数 | **31** | **31** | **28** |
| 弱いスイート数（3DES / RC4 / NULL / SHA-1 CBC） | **11** | **11** | **10** |
| `TLS_RSA_WITH_3DES_EDE_CBC_SHA` | あり | あり | **なし** |
| `TLS_DHE_RSA_WITH_AES_*_GCM_*`（2 種） | あり | あり | **なし** |
| SHA-1 CBC スイート（6 種） | あり | あり | **あり** |
| NULL 系（`TLS_RSA_WITH_NULL_*` / `TLS_PSK_WITH_NULL_*`。計 4 種） | あり | あり | **あり** |
| RC4 | なし | なし | なし |

Microsoft の公表内容とも整合する——[TLS Cipher Suites in Windows Server 2022](https://learn.microsoft.com/en-us/windows/win32/secauthn/tls-cipher-suites-in-windows-server-2022)（確認日 2026-08-02）は既定一覧に 3DES・DHE・NULL を含めたうえで、「**Windows Server 2022 23H2 以降では `TLS_RSA_WITH_3DES_EDE_CBC_SHA`・`TLS_DHE_RSA_WITH_AES_256_GCM_SHA384`・`TLS_DHE_RSA_WITH_AES_128_GCM_SHA256` が既定で無効**」と注記している。2025 側の実測はこの 3 種がちょうど抜けた形であり、文書と実測が一致する。

#### 決定への反映

- **決定 1 の差の内訳 ② を確定させた**。**Server 2019 と Server 2022 の既定は完全に同一**であり、「対応環境は推奨環境より暗号強度が劣る」とは**言えない**。区分間の差は Server 2025 で 3DES と DHE 2 種が外れる 1 点のみである
- **NULL についての記述を訂正した**（改訂 7 の書き方は過大だった）。NULL 系は既定の一覧に載るが、Microsoft は「アプリケーションが明示的に要求した場合にのみ使われる」と定めており、**Yagura は要求しない**。一覧に載っていることを「平文で成立しうる」と読ませない書き方に改めた。README のセキュリティ前提も同じ向きに直した
- **残る注意は全区分共通**である: 既定では **SHA-1 の CBC スイート**（6 種）が有効なままで、それしか提示しない相手とはその強度で成立する。3DES は 2019 / 2022 でのみ該当

#### 委任事項 1 の残条件

**残条件 B は本改訂で消えた。**残るのは A（閲覧 HTTPS と `tcp_connection.faulted` を含む main ビルドでの §A-1・§A-3 の再測）のみで、[#494](https://github.com/Yanai-Taketo/Yagura/issues/494) に残す。

### 改訂 9（2026-08-02）: 委任事項 6（MSI の起動条件）を実装。決定 6 は不変

決定 6（対応下限を MSI の起動条件で機械的に強制する）を `installer/Package.wxs` へ実装した（[#496](https://github.com/Yanai-Taketo/Yagura/issues/496)）。**決定そのものは変えていない**——実装方式と、実装時に確定させると委任していた 4 点の答えを記録する。

#### ① 使用するプロパティと比較方法（実ビルドでの確証）

委任事項 6 は「`VersionNT` / `WindowsBuild` が実ビルド番号を返さない可能性」を最大の risk として挙げ、**机上調査に留めないこと**を条件にしていた。installer-e2e の実ビルドで実測した結果は次のとおりで、**懸念は現実だった**。

| プロパティ | windows-2022 | windows-2025 |
|---|---|---|
| `VersionNT` / `VersionNT64` | 603 | 603 |
| `WindowsBuild` | **9600** | **9600** |
| `YAGURA_OS_BUILD`（本実装のレジストリ読み） | **20348** | **26100** |

**`WindowsBuild >= 17763` と書いていれば両方とも false になり、対応環境を含む全環境で新規インストールとアップグレードが拒否されていた。** 委任事項 6 が想定した最悪の失敗様式そのものである。

採った方式は `HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\CurrentBuildNumber`（REG_SZ）の `RegistrySearch`。MSI は整数へ変換可能な文字列プロパティを整数と数値比較する（[Conditional Statement Syntax](https://learn.microsoft.com/en-us/windows/win32/msi/conditional-statement-syntax)「Comparison of an integer with a string or property value that cannot be converted to an integer is always false」。確認日 2026-08-02）。

#### ② 条件式（3 項。どれを落としても事故になる）

```
NOT YAGURA_OS_BUILD OR YAGURA_OS_BUILD >= 17763 OR REMOVE~="ALL"
```

- **`NOT YAGURA_OS_BUILD`（読めなければ通す = fail-open）**: 判定材料を得られないことと「対応外 OS である」ことは別である。ここを fail-closed にすると**測定の失敗がそのまま全環境ブロックに化ける**。本条件はセキュリティ境界ではなくサポート表明のゲートであり、被害の非対称性（全環境の更新停止 ≫ 対応外 OS への誤インストール）から fail-open を採る
- **`REMOVE~="ALL"`（アンインストールは常に許す）**: `LaunchCondition` は削除シーケンスでも評価される。これが無いと**対応外 OS の利用者は製品を消すこともできなくなる**。委任事項 6 の必須項目には挙がっていなかったが、同じ「全環境ブロック」系の事故である

#### ③ 拒否メッセージ

「なぜ入れられないか（検出したビルド番号を含む）」「対応下限と推奨」「詳細と移行相談の導線」を含める。**`LaunchCondition` の Description 列は 255 文字が上限**で、超えると ICE03 でビルドが落ちる（当初案 258 文字が実ビルドで落ちた）。移行相談の導線は字数の都合で README 側へ委ねた。

#### ④ Server Core — 判別できるが拒否しない

`HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\InstallationType`（`Server Core` / `Server` / `Client`）で判別できる。**判別できても拒否しない**方針とする——決定 1 が Server Core を対応外に置いた理由は「未検証だから」であって動作しないからではなく、ADR-0004 決定 2 は設定ファイル手編集による管理経路を明示的に設計している。ここで拒否すると**その経路を試すこと自体を封じ、既存利用者の更新も止める**。

#### 検証の現況

委任事項 6 は「4 区分 × 2 経路」を求め、**推奨・対応区分で正常に通ることを、対応外での拒否確認と同格の必須項目**としていた。

| 経路 | 状況 |
|---|---|
| 推奨区分（2022 / 2025）× 新規インストール | ✅ installer-e2e で成功（両ランナー） |
| 推奨区分（2022 / 2025）× メジャーアップグレード | ✅ installer-e2e のアップグレード検証で成功（両ランナー） |
| 対応区分（Server 2019）× 両経路 | ⬜ lab 未実施（#496 に残す） |
| 対応外区分 × 両経路 | ⬜ lab 未実施。**拒否が出ることの確認**（#496 に残す） |

**通過側は CI で常時検証される**（installer-e2e は `installer/**` の変更で走る）。したがって「条件式の誤りで全環境が更新不能になる」という最悪ケースは、リリース前に機械的に検出される。残るのは拒否側の実地確認である。

### 改訂 10（2026-08-08）: 委任事項 1 の残条件 A を完了。決定 1・決定 2 の表明範囲を確定

オーナー lab で第 2 回検証を実施した（Server 2019 Standard Evaluation 10.0.17763.3650、**`origin/main = 41be3fd`（2026-08-05）からビルドした MSI**）。改訂 7 が残条件 A として送った §A-1（観測性判定）・§A-3（閲覧 HTTPS）を完了する。

#### 残条件 A の結果 — 合格

**開始前チェックで、改訂 7 の判断が実測で裏付けられた**。main ビルドには `yagura.ingestion.tcp_connection.faulted`（平易語「取りこぼしの疑い（接続が想定外の理由で終了）」）と閲覧 HTTPS 画面が存在し、**v0.5.0 では両方とも欠落していた**。「検証対象の版が v1.0 の版でない」という改訂 7 の問題提起は正しく、手順書へ入れた「MSI は main からビルドする」の指定も有効だった。

§A-1・§A-2・§A-3 とも TLS 1.2 で成立。**§A-3（閲覧 HTTPS）も実測で確認済み**となり、改訂 7 が「コード経路の同一性で実質的に被覆されている」とした推論は実測で裏付けられた。

**§A-4 の観測性は「条件付き合格」**である。TLS 1.1 のみを提示するクライアントで 5 回失敗させたところ、**`faulted` は増えず、イベントログに 5/5 が警告として記録**された。原因は仕様どおり——ハンドシェイク失敗は専用の catch で処理され、`faulted` は最後の汎用 catch 専用のためである。**「失敗が観測できない」という不合格分岐には該当しない**。ただしハンドシェイク失敗の計器が状態画面に出ない点は別途扱う（[#509](https://github.com/Yanai-Taketo/Yagura/issues/509)）。

**委任事項 1 の残条件はこれで全て解消した。**

#### 決定 2 の表明範囲 — SQL Server 2019 のゲートを開ける

決定 2 は SQL Server 2019 / 2025 に「実測ゼロ。委任事項 2 のゲートを通すまで README で表明しない」というゲートを掛けていた。

- **SQL Server 2019 Express 15.0.2000.5**（サーバ照合順序 Japanese_CI_AS）で、接続・スキーマ自動作成・照合順序検査・昇格の通し・保持期間削除・DB サイズ計測・負荷 1 回の**7 項目すべてが合格**した。**ゲートを開け、README へ「対応」として転記する**
- **SQL Server 2025 は未検証のまま**である（lab に未インストール）。**ゲートは閉じたままとし、README では「検証中」を維持する**

#### 決定 4（ハードウェア要件）に使える実測値

決定 4 は SQL Server 版の算出式を実測に委ねていた。今回得られた値:

| 測定項目 | 実測値 |
|---|---|
| 1 レコードあたりの所要（メッセージ長 55 B × 10,200 件、非クラスタ化インデックス 3 本込み） | **reserved 324 B / used 309 B** |
| データファイル | 8.00 MB 確保 / 6.31 MB 使用 |
| **ログファイルの自動拡張** | 一括投入で **8 MB → 72 MB** まで拡張 |
| 受信性能（TCP 514） | 18,533 msg/s で 10,000 件投入 → 10,000 件保存・全カウンタ 0 |

**ログファイルの拡張はサイジング指針に含める価値がある**——データファイルの見積もりだけでは足りず、一括投入でログが 9 倍に膨らむ。決定 4 の算出式を確定する際（M-6）に反映する。

**昇格反映時の受信断は 31.3 秒**（うちサービス停止に 30.3 秒）だった。2026-08-02 の実測（約 1.7 秒）と大きく異なるため、**この値は環境差が支配的であり公称値には使えない**。

#### 検証で判明した製品側の欠陥

| 内容 | 追跡 | 扱い |
|---|---|---|
| **証明書選択の候補一覧が 3 画面すべてで描画されない**（`27b0ce9` からの回帰） | 本 PR で修正 | **v1.0 公開の阻害要因だった**。新規利用者は UI から opt-in 強化 3 点のいずれも証明書を選べなかった |
| ハンドシェイク失敗の計器が状態画面に出ないのに、画面は「計器に出る」と案内 | [#509](https://github.com/Yanai-Taketo/Yagura/issues/509) | 文言は本 PR で是正。計器の表示追加は判断待ち |
| 昇格直後の初回起動で保持期間キャッチアップが必ず失敗（#501 と同じ根本原因） | [#510](https://github.com/Yanai-Taketo/Yagura/issues/510) | |
| ACL 自動付与の警告が偽陽性（既に権限がある場合にも出る） | [#511](https://github.com/Yanai-Taketo/Yagura/issues/511) | |
| 管理ホームに「再起動待ちの変更」が表示されない | [#512](https://github.com/Yanai-Taketo/Yagura/issues/512) | |
| 秘密鍵 ACL の案内コマンドが PowerShell で失敗する引用符だった | 本 PR で修正 | #503 の改善自体は「効いている」ことも確認された |

#### 委任事項 6 の検証状況について

第 2 回検証では「MSI の対応 OS 下限」の拒否側も見る計画だったが、**`origin/main` にその起動条件は存在しなかった**ため実施不能だった。実装は本 PR（[#504](https://github.com/Yanai-Taketo/Yagura/pull/504)）にあり、**マージ前だった**のが原因である。検証依頼側（本セッション）の手配ミスであり、lab の判断は正しい。委任事項 6 の実機検証は本 PR のマージ後に再手配する（[#496](https://github.com/Yanai-Taketo/Yagura/issues/496)）。

### 改訂 11（2026-08-08）: 委任事項 5・10 を判断。決定は不変

どちらも「やるかどうかを決める」委任であり、[#497](https://github.com/Yanai-Taketo/Yagura/issues/497) で 1 つに束ねられていた。**いずれも「実施しない」と判断し、代わりに何を明示するかを定める。**

#### 委任事項 5: SQL Server 実インスタンスでの CI 実走 → **採らない**

判断の対象は「SQL Server 適合テストを CI で走らせるか」ではない（それは全 PR で既に実走している）。**対応区分内の実インスタンス（Express）での追加実走の要否**である。

**採らない理由**:

1. **適合テストが検証しているのは provider の契約**——T-SQL の方言・スキーマ移行・照合順序検査であり、**LocalDB は同じエンジンでこれらを同一に実行する**（`SERVERPROPERTY('EngineEdition')` も Express と同じ `4` を返すため、Express 容量接近警告の判定経路も継続検証されている）
2. **LocalDB と実インスタンスで実際に違うのは実行形態**——ユーザーセッションに紐づく起動モデル・ネットワークプロトコルの不在・サービスアカウントでの接続。**これらは適合テストが触れていない領域**であり、実インスタンスを CI に立てても適合テストの被覆は増えない
3. **その実行形態の差は 2026-08-08 の lab で実測済み**（改訂 10）。SQL Server 2019 Express の実インスタンスに対し、接続・スキーマ自動作成・照合順序検査・昇格・保持期間削除・DB サイズ計測・負荷の 7 項目が通っている。**決定 5 が対応区分に定めた「単発の実機検証」の水準を満たす**
4. コストは全 PR に乗る（セットアップ時間・ランナー資源）

**代わりに明示すること**（決定 2 が既に求めているものを、2 層構造として具体化する）:

ADR-0006 基準 1 の証拠として SQL Server provider の検証記録を提出する際は、**継続検証と単発検証を分けて書く**。

| 層 | 実行形態 | 内容 | 頻度 |
|---|---|---|---|
| 継続 | **LocalDB**（決定 2 で運用形態としては対応外） | 適合テスト（provider 契約） | 全 PR・blocking |
| 単発 | **SQL Server 2019 Express の実インスタンス** | 接続・スキーマ・照合順序・昇格・保持期間・サイズ・負荷 | lab（2026-08-08 実施） |

「常設・blocking な自動検証が対応外の実行形態の上にのみ成立している」ことを**伏せない**（決定 2 の要求）。同時に、**実行形態の差は単発検証で埋めてある**ことも併記する——片方だけを書くと実態より弱くも強くも見える。

#### 委任事項 10: Server 2022 での lab 実施 → **実施しない。非対称を明示する**

**前提は事実である**。AD 連携認証・gMSA の実機検証（ADR-0015 の SEC-14 受け入れ、2026-07-11 / 07-24）は**すべて Windows Server 2025 `10.0.26100` の DC 上**で行われており、Server 2022 では踏まれていない。

**実施しない理由**:

1. **Yagura 側の AD 依存経路は OS バージョンに依存しない**——Windows 統合認証は Negotiate/Kerberos、gMSA は SCM の service logon であり、いずれも Yagura が OS バージョンで分岐する箇所を持たない
2. **OS バージョン差が出るのはサービス登録・ACL・MSI の面**であり、そこは **installer-e2e が Server 2022 で常時検証している**（#491 の matrix 化以降。本 ADR の委任事項 6 の起動条件も 2022 で通ることを確認済み）
3. AD lab は DC + メンバー機の 2 台構成を要し、**推奨区分の 2 バージョン目のためだけに複製するコストが、上記 1・2 を踏まえた残存リスクに見合わない**

**ただし「OS ビルド差は起きない」とは言えない**。ADR-0015 改訂履歴 2 は、`SeServiceLogonRight` が**検証済みビルド 26100 では不要**であることを実測で確定し、当初の「必須」結論を撤回している。**これは gMSA 経路に実際のビルド依存があった事例**であり、非対称を「理論上の穴」として片付けられない根拠になる。

**代わりに明示すること**:

- **利用者向けの手当ては既にある**——`docs/guides/gmsa-service-account.md` §2.4 は「26100 では不要を実測確定。**未検証ビルドへの備えとして付与を推奨**」と書いており、既知のビルド差はこの推奨で中和されている
- **要件表には書かない**。「AD 系の裏付けは Server 2025 のみ」を README のシステム要件表に載せると、**推奨区分の中に等級があるかのように読める**——実際には約束する水準は同じで、裏付けの厚みが違うだけである。等級の話に化けるほうが誤解が大きい
- **本 ADR に非対称として記録し、再評価トリガに載せる**（下記）

#### 再評価トリガへの追加

- **Server 2022 から gMSA・AD 連携認証に関する不具合報告が来た場合**: 委任事項 10 を再開し、Server 2022 の AD lab を実施する
- **別の理由で AD lab を立て直す場合**: そのとき Server 2022 も対象に含める（単独では割に合わないが、ついでなら安い）

### 改訂 12（2026-08-16）: 委任事項 6 を完了。決定 6 が第 3 の OS で裏づけられ、アップグレード検証が初めて有効な形で成立した

第 4 回 lab 検証（2026-08-16。main `220f23a`。報告全文: [`installer/lab/server2019-support-validation-result-2026-08-16.md`](../../installer/lab/server2019-support-validation-result-2026-08-16.md)）の結果を記録する。**決定は不変。**

本回で初めて**対応外 OS の実機**（Windows Server 2016、ビルド 14393）が用意でき、起動条件の拒否側を実測できた。

#### 委任事項 6（MSI の起動条件）= **完了**

委任事項 6 は「4 区分 × 2 経路（新規 / アップグレード）の検証」を必須項目に含めており、改訂 9 の実装時点では**拒否側が未実測**のまま残っていた。本回でその穴が埋まった。

| 経路 | 環境 | `YAGURA_OS_BUILD` | 結果 |
|---|---|---|---|
| 新規（`/qn`） | Server 2016 = 14393 | 14393 | **拒否**（exit 1603） |
| 新規（`/qb`。UI シーケンスも評価） | Server 2016 = 14393 | 14393 | **拒否**（exit 1603） |
| 新規（`/qn`） | Server 2019 = **17763**（下限そのもの） | 17763 | 成功（exit 0） |
| アップグレード | Server 2019 = 17763 | 17763 | 成功（exit 0） |
| アンインストール | Server 2019 = 17763 | 17763 | 成功（`LaunchConditions` 戻り値 1） |

拒否メッセージは**検出値と下限の両方**を含んでいた（改訂 9 で 255 文字制限に合わせて詰めた文面が、実機で意図どおり出ることの確認）:

> この OS には Yagura をインストールできません(OS ビルド **14393**)。対応は OS ビルド **17763** 以上(…)

拒否時に `Program Files\Yagura`・サービス・ARP エントリ・`ProgramData\Yagura` のいずれも作られないことも確認済み。

**下限ちょうど（17763）で通ったことは、比較演算子の off-by-one が無いことの直接の証拠**である。CI のランナーは 20348 / 26100 にしか到達しないため、`&gt;` と `&gt;=` の取り違えは**この 1 台でしか検出できない**。

#### 決定 6（レジストリの実値を直接読む）が第 3 の OS で裏づけられた

改訂 9 は `VersionNT` / `WindowsBuild` が実ビルドを報告しないことを windows-2022 / windows-2025 の 2 ランナーで実測していた。本回、**Server 2016 でも同じ値に張り付く**ことが確認された。

| プロパティ | Server 2016 実測 | 実際のビルド |
|---|---|---|
| `VersionNT` / `VersionNT64` | 603 | — |
| `WindowsBuild` | 9600 | — |
| `YAGURA_OS_BUILD`（レジストリ読み） | **14393** | 14393 |

重要なのは**誤った実装が「たまたま動いて見える」向き**である。`WindowsBuild >= 17763` を採っていた場合、対応外 OS は 9600 で拒否されるため**拒否側のテストだけは通ってしまう**——一方で**対応環境も同じ 9600 で拒否され、全利用者が導入・更新不能になる**。3 つの OS で同じ値に張り付くことが確認された以上、これは環境依存の偶然ではない。

#### アップグレードでのバイナリ置換 = **確認**（[#479](https://github.com/Yanai-Taketo/Yagura/issues/479) / [#514](https://github.com/Yanai-Taketo/Yagura/issues/514)）

**第 1・2 回 lab のアップグレード検証は無効だった**。両回とも `-p:YaguraVersion=` を渡してビルドしており、#514（MSBuild のグローバルプロパティが子の `dotnet publish` へ伝播しない）により**版だけ上がって中身が旧版**の MSI を比べていた。本回は `Directory.Build.props` を書き換えてビルドし、初めて有効な比較になった。

- 版 1 = ProductVersion 0.9.1 / 収録 `Yagura.Host.exe` FileVersion **0.9.1.0**、版 2 = 0.9.2 / **0.9.2.0** —— **一致を確認**
- アップグレード後の実ファイルは Yagura 自身の 5 アセンブリすべてが 0.9.2.0。ログは `Existing file is a lower version` で `Overwrite`（#479 の症状だった `equal version` ではない）
- ARP は 0.9.2 の 1 エントリのみ・サービスは Running
- **傍証**: 検証開始時の lab 機は ARP の DisplayVersion が `0.5.1` なのに実バイナリが `0.5.0.0` という、#514 の症状そのものの状態だった

#### 判明した欠陥 —— [#511](https://github.com/Yanai-Taketo/Yagura/issues/511) の修正が誤警告を作っていた

秘密鍵 ACL の自動付与について、**正常な構成の起動のたびに 3 リスナ分の「自動付与できませんでした（理由: (null)）」が出る**ことが実測された（権限は実際に付いており TLS は成立していた）。

原因は #511 の修正そのものである。「既に権限がある場合は監査記録を残さない」を `if (Succeeded && !WasAlreadyGranted) { 監査 } else { 警告 }` と書いたため、**「既に権限がある」という第三の結果が else 側へ落ちた**——#511 が黙らせたかった当の経路が、そのまま警告に化けていた。

**教訓として記録する**: 「監査に残すか」と「警告を出すか」は独立した問いであり、一本の if / else で表すと、どちらでもない第三の結果が必ずどちらかへ落ちる。修正では素の真偽値の公開をやめ、問いごとに名前の付いた述語だけを残した（同じ分類を呼び出し側で組み立て直すとコンパイルが通らない）。同型のコードが 3 箇所へ複写されており、**同じ誤りが 3 つ同時に入った**ことも、判断をデータ側へ寄せる根拠になっている。

#### そのほかの確認結果

| 項目 | 結果 |
|---|---|
| 診断用計器の表示（[#509](https://github.com/Yanai-Taketo/Yagura/issues/509)） | **合格**。TLS 1.1 で 5 回失敗 → 計器 5、再起動 → 0（見出しの「再起動でリセットされます」どおり）。平易語も登録済み |
| 昇格の接続検証の照合順序検査（[#515](https://github.com/Yanai-Taketo/Yagura/issues/515)） | **合格（回帰なし）**。SQL Server 2019 Express で成功。サーバ既定は `Japanese_CI_AS` で必要な照合順序とは別物のため、「たまたま既定と一致して通った」のではない |
| 証明書選択の候補一覧（3 画面） | **修正確認**。#488 以降の回帰は解消 |
| `icacls` 案内の引用形（[#503](https://github.com/Yanai-Taketo/Yagura/issues/503)） | **修正確認**。現行形が PowerShell で exit 0、前回の形は exit 87 を再現 |
| 昇格後の「再起動待ちの変更」（[#512](https://github.com/Yanai-Taketo/Yagura/issues/512)）・`[retention-catchup-query-failed]`（[#510](https://github.com/Yanai-Taketo/Yagura/issues/510)） | **未観測**。どちらも昇格の**切替実行後**でないと現れないが、本回の指示は接続検証までだった。lab が構成変更を独断で行わなかった判断は妥当。次回に明示指示として渡す |

#### 依頼範囲外の観測 —— TLS 受信の受信許可規則

TLS 受信（TCP 6514）を有効化した状態で `[firewall-rule-mismatch]` が出ており、警告文自身が「この状態では受信がファイアウォールで破棄され、Yagura のカウンタにも現れません」と述べている。**警告としては正しく機能している**が、opt-in の TLS 受信を有効にした利用者が規則の追加まで到達できるかは別問題であり、[#518](https://github.com/Yanai-Taketo/Yagura/issues/518) として切り出した（本 ADR の対応区分の判断には影響しない）。

#### 手順書の誤り（次回への申し送り）

- アンインストールは**アップグレード後の版の MSI**（または ProductCode 指定）で行う。アップグレードで ProductCode が変わるため、旧版の MSI では `1605`（ERROR_UNKNOWN_PRODUCT）になる
- MSI を 2 版作る際は `Directory.Build.props` の `<YaguraVersion>` を書き換える。`-p:YaguraVersion=` は #514 の修正後も**手順としては使わない**（同じ落とし穴を再現しないため）
- 拒否ダイアログの**目視確認だけが残っている**（本回は WinRM 経由のため対話デスクトップが無く、`/qb` のログから同一文面を全文取得した）。RDP が使える回に消化する
